---
description: "Task list for TypeScript Pattern Generation Service"
---

# Tasks: TypeScript Pattern Generation Service

**Input**: Design documents from `/specs/001-typescript-pattern-mcp/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md

**Tests**: Test tasks are included and are **not optional here**. Constitution Principle III makes
compile- and test-verification a non-negotiable product guarantee, and SC-001 through SC-003 are
stated as continuously measured with zero tolerated exceptions. Tests are the feature, not a wrapper
around it.

**Organization**: Grouped by user story so each is independently implementable and testable.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependency on incomplete work)
- **[Story]**: Which user story the task serves (US1–US6)
- Exact file paths are given in every task

T001–T098 are numbered in execution order. T099 and above were added by the cross-artifact analysis
pass and are placed in the phase where they execute rather than appended at the end, so IDs remain
stable references while ordering stays readable.

## Path Conventions

Single published package with three entry points, per plan.md: `src/engine/` (pure generator),
`src/mcp/` (protocol adapter), `src/cli/` (terminal adapter), `data/` (catalog), `test/` (harnesses).

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Get the toolchain, boundaries, and test topology in place. The scaffold from earlier work
(pnpm, tsdown, vitest, tsconfig) already exists — these tasks extend it rather than recreate it.

- [X] T001 Add runtime dependencies `@modelcontextprotocol/server@2.0.0` and `zod@4.4.3` to `package.json` — note the SDK is *not* `@modelcontextprotocol/sdk`, which is the older 1.x monolith still tagged `latest`
- [X] T002 Pin `typescript@7.0.2` and `prettier@3.9.6` with exact versions (no caret) in `package.json`, and set `engines.node` to `>=20` per the Toolchain Pinning section of the constitution
- [X] T003 [P] Create oxlint config `.oxlintrc.json` including the import-boundary rule that forbids any module under `src/engine/` or `src/index.ts` from importing MCP packages (Principle X), and add a `lint` script. Not ESLint — `typescript-eslint` caps at `typescript <6.1.0` against our pinned 7.0.2, per research.md §12
- [X] T004 [P] Create the source tree skeleton per plan.md: `src/engine/{catalog,patterns,options,render,format,verify,provenance}/`, `src/mcp/{tools,resources,transports}/`, `src/cli/`, `data/patterns/`
- [X] T005 [P] Configure Vitest projects for `unit`, `contract`, `golden`, `determinism`, and `parity` suites in `vitest.config.ts`, with matching directories under `test/`
- [X] T006 Add a composite `check` script to `package.json` chaining the stages that exist now — lint, typecheck, all five test projects, and build. Build is included because declaration emit can fail where `tsc --noEmit` passes, which is how the earlier tsup incompatibility surfaced. Catalog validation and conformance join in T011 and T083 respectively: a stage is wired into the gate by the task that creates the thing it checks, never before, or the gate is red from here to T083 and every rule that depends on it is unenforceable. This is the single gate referenced by quickstart.md

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The generation engine's shared machinery. Every user story depends on it.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [X] T007 [P] Define Zod schemas for `Option`, `LegalityRule`, and `Pattern` in `src/engine/catalog/schema.ts` per data-model.md, including the identifier and length rules
- [X] T008 [P] Define the `Conventions` schema with strictest-reasonable defaults in `src/engine/options/conventions.ts` per data-model.md
- [X] T009 Generate `data/schema.json` from the Zod schemas in `scripts/emit-catalog-schema.ts` so the published schema cannot drift from the runtime validator. `pnpm schema:check` joined the gate in this task. Note: JSON Schema cannot express the cross-field refinements (enum `default` ∈ `values`, no self-edge), so this artefact is necessary but not sufficient — T011 remains the authority on admissibility
- [X] T010 Implement catalog loading and shard merging with name-uniqueness and `relatedPatterns` reference checks in `src/engine/catalog/load.ts`. Split into a pure `buildCatalog(shards)` and an I/O `loadCatalog(dir)` that sorts `readdir` output, so merge rules are testable without fixtures and traversal order cannot leak from the filesystem
- [X] T011 [P] Write the catalog validator script `scripts/validate-catalog.ts` enforcing schema conformance, presence of `provenance`, rejection of NonCommercial and NoDerivatives license terms, and the advisory-entry invariants (FR-036, SC-012). Note: T007 made the licence allowlist, the advisory invariants, and self-edge rejection structural in `PatternSchema`, so this script enforces them by parsing rather than by re-checking them; its remaining own work is cross-shard name uniqueness and `relatedPatterns` resolution (T010) plus the `{category}.json` file-name convention, which relates a file's name to its contents and so cannot be expressed in any per-document schema. `pnpm catalog:check` joined the gate here. An empty catalogue passes on purpose; the tier-1 minimum count is asserted by the task that completes the tier, where it can actually be satisfied
- [X] T012 [P] Implement render helpers `dedent`, `indent`, `when`, `joinLines`, and `sortBy` in `src/engine/render/helpers.ts` — tagged template literals only, no template engine, so option renames break the build. Interpolation is indentation-aware: a multi-line value is re-indented to its placeholder's column, and CRLF is normalised so output does not vary with the checkout that built the generator
- [X] T013 [P] Implement identifier validation, the reserved-word denylist, and string length caps in `src/engine/options/identifiers.ts` (FR-032). The denylist must also cover identifiers the requested pattern itself emits, since a domain name colliding with a generated helper produces uncompilable output just as surely as a keyword does (spec edge case). Returns a result rather than throwing; mapping a broken rule onto a caller-correctable error belongs to T014. The accepted shape is an allowlist pattern, which also excludes path separators and traversal (FR-033)
- [X] T099 [P] Implement the name derivation table in `src/engine/options/names.ts` plus its data in `data/names.json`: plural forms with an explicit irregular-exception list, casing variants, and file-name stems. No pluralization library — a version bump in one would silently rename members across every consumer's regenerated output (FR-040, Principle I). Split like the catalogue: `deriveNames` is pure and takes a loaded table, `loadNameTable` holds the only I/O, so generation never reads the filesystem. Ambiguous suffixes are checked before the confident rules, since `-us` also ends in `-s`
- [X] T100 [P] Refuse names the derivation table cannot resolve confidently, stating the rule, rather than guessing a plural (FR-041)
- [X] T014 Define the error taxonomy from contracts/engine-api.md in `src/engine/errors.ts`, distinguishing caller-correctable errors from `VerificationError`. Adapters branch on a `correctable` flag and a stable `code` rather than on the concrete class or message text. `VerificationError` takes its correlation id rather than generating one, since generating it would need a clock or random source on the engine's own error path
- [X] T015 Implement option resolution — defaults applied, unknown option names rejected, key order normalized — in `src/engine/options/resolve.ts`, following the fixed validation order in data-model.md. The order is asserted by tests that break two rules at once, and all three ordering guarantees (unknown-option iteration, key normalisation, variant before identifiers) were mutation-tested. `variant` is checked at step 3b: it is a request field rather than a declared option, but it is a value-space check like the others
- [X] T101 Validate the `variant` input against the requested pattern's declared `variants` in `src/engine/options/resolve.ts`, refusing an undeclared variant with the declared list (FR-013)
- [X] T016 Implement the legality rule evaluator in `src/engine/options/legality.ts`, evaluating in declared order with first-match-wins so the error for a given input is deterministic. Exposes `findViolation` alongside `evaluateLegality` so discovery can report what a hypothetical option set would break without catching an error. A rule naming an option the pattern lacks does not fire: a miswritten catalogue entry must not become a refusal that blames the caller
- [X] T017 Implement canonical serialization and the deterministic `optionsHash` in `src/engine/provenance/hash.ts`, excluding generator and toolchain versions (FR-021). `variant` is included, since data-model.md's field list predates it and omitting it would give two materially different bundles the same provenance. Whether `conventions` should be covered is recorded as an open question in blockers.md and needs a decision before T057 emits headers
- [X] T018 Implement the Prettier wrapper with caller-config merge in `src/engine/format/prettier.ts`, warming the instance for reuse. Caller config is an allowlist, not a pass-through: a `plugins` entry is `import()`ed by Prettier (verified), and `endOfLine: "auto"` makes output depend on the input's line endings. `resolveConfig` is never called, since it walks the filesystem for a `.prettierrc`. Corrects the T008 comment that said Prettier should validate its own options
- [X] T019 Implement the verification virtual file system in `src/engine/verify/vfs.ts` — `readFile` must be tri-state and return `undefined` (not `null`) for lib paths, or lib resolution silently fails and produces a cascade of misleading "Cannot find name" diagnostics. Unknown non-lib paths return `null` rather than `undefined`, unlike `createVirtualFileSystem`; the measured consequence of the latter is that a real `tsconfig.json` on disk is opened and supplies the compiler options, so a bundle could be certified under settings we never chose. `fileExists` is tri-state for the same reason. Compiler-backed tests in `test/unit/vfs-compiler.test.ts` pin both the lib cascade (TS2583) and the tsconfig behaviour; both are mutation-verified. A `readThrough` seam is in place for T104/T105, where binding-only bundles need types for packages we do not emit
- [X] T020 Implement warm async typechecking via `typescript/unstable/async` in `src/engine/verify/typecheck.ts`: one long-lived API instance, `fileChanges.changed` on each snapshot, and restart-on-crash for the compiler subprocess. The async variant is mandatory — the sync variant pins the event loop completely (confirmed: 15 sequential checks left 148 event-loop ticks). Measured: ~130ms cold, ~13ms warm. Three findings drove the design. (1) Omitting the change summary makes the compiler serve a **stale verdict** — a broken bundle measured clean — so `createMutableVerificationFileSystem().replace()` derives the summary from the same tree the compiler reads rather than trusting a caller to describe it. (2) `invalidateAll` does **not** reload project configuration; only naming the tsconfig in `changed` does, which the mechanical diff does whenever its bytes differ. (3) A killed compiler does not reliably reject the request in flight, so there is a deadline, and failure abandons the compiler instead of closing it — closing ends the pipe under a queued write and produces an unhandled stream error from our own recovery path. Restart-on-crash verified for every crash except a same-tick write race, recorded in blockers.md. Also carries `compilerOptionsFor(conventions)`, verified against the real compiler across the whole moduleStyle × importExtensions × strictness matrix — a wrong mapping does not fail loudly, it reports "cannot find module" on every import of a good bundle. Corrects a note I left on this task last iteration: the *input* `fileChanges` is `{ changed, created, deleted }` as research.md says; `changedFiles`/`deletedFiles` is the per-project *output* shape
- [X] T021 Implement the stable TypeScript 6.x `createProgram` fallback behind a flag in `src/engine/verify/typecheck-fallback.ts`, so the verification gate survives an upstream break in the unstable API. `typescript@6.0.3` added as a runtime dependency aliased `typescript-stable`, pinned exactly, so it coexists with the 7.x compiler. Both paths implement a shared `Verifier` interface; `createVerifier({ engine })` in `src/engine/verify/index.ts` selects between them by **parameter, not environment variable** — `process.env` in the engine would make behaviour depend on ambient state and put the choice beyond a test's reach. Options reach the classic API through `convertCompilerOptionsFromJson`, which is load-bearing: passing the JSON form directly yields a program with no module resolution, and mutation-testing that line fails 14 assertions. Agreement with the fast path is asserted across the whole moduleStyle × importExtensions × strictness matrix, since a fallback that disagrees replaces the gate rather than preserving it. **Also corrects T002**: `typescript` was pinned into devDependencies, but `src/engine/verify/typecheck.ts` imports it at runtime, so a consumer install would have shipped without a compiler. Now a runtime dependency alongside the alias
- [X] T022 Implement the generated-test executor with a hard timeout and sandboxing in `src/engine/verify/run-tests.ts`; it must never execute caller-supplied content (FR-034). Sandboxing is Node's permission model (`--permission --allow-fs-read=<dir>`), which denies filesystem writes, child processes, and native addons; the environment passed to the child is empty, so no configuration or token reaches it. Each test file is invoked **directly rather than through `node --test`**, because `--test` spawns a child per file and would require `--allow-child-process` — handing the sandboxed code the ability to spawn anything. Two traps encoded: the permission model checks the *realpath*, and on macOS the temp directory is behind a symlink, so granting the unresolved path denies every read; and the bundle must be transpiled to CommonJS with `rewriteRelativeImportExtensions` before it can run, because the default `importExtensions: "js"` convention emits `./thing.js` against a `thing.ts` file — TypeScript resolves that, Node reports `ERR_MODULE_NOT_FOUND`. All three extension styles verified executable. Timeout uses SIGKILL, since a busy loop declines SIGTERM. The three sandbox assertions were mutation-verified by removing `--permission`. Two decisions escalated to blockers.md: execution costs ~60ms, which puts plan.md's 50ms p95 out of reach for any bundle with tests; and only `node:test` can currently be executed, so the `vitest` default needs a shim before the generator emits its first test
- [x] T023 [P] Write unit tests for every foundational module in `test/unit/` — options resolution, legality ordering, identifier rejection, hash stability, VFS lib fallthrough. Every module already had a test file, since each was built test-first, so this task became an audit for gaps rather than a writing exercise. `@vitest/coverage-v8` was added to answer that objectively (`pnpm test:coverage`); it moved from 96.8% to 97.5% of statements and 86.1% to 87.3% of branches, and found three things worth reporting.

  **A real defect in `names.ts`.** A single-word acronym lost its casing when pluralised: `ID` gave `Ids`, `URL` gave `Urls`, `API` gave `Apis`, `userID` gave `userIds`. `capitalise` preserved a word that was entirely uppercase, but a pluralised acronym is not — the appended `s` makes `IDs` mixed case — so it fell through to the branch that lower-cases the tail. Compound names were unaffected (`URLShortener` → `URLShorteners`) because only the final word is pluralised, which is why no existing test caught it. Fixed with an `ACRONYM_PLURAL` guard requiring two leading capitals, so `Status` still normalises. Reverting the guard fails seven assertions.

  Note the two pluralisation paths now disagree on all-caps input by design: the exception table upper-cases the whole entry (`CHILD` → `CHILDREN`, correct for a shouted word) while the default rule preserves the acronym and lowers only the suffix (`ID` → `IDs`, correct for an acronym). Nothing can distinguish an acronym from a shouted word, and each rule is right for the input that reaches it, so both are pinned by name in the tests.

  **A loose hermetic boundary in the fallback.** `directoryExists` delegated to the real filesystem for anything outside the bundle root, which is how module resolution walks for `node_modules`. Nothing leaked in practice only because the root `/verify` does not exist on disk — an accident, not a design. Tightened to answer for the bundle root and the compiler's lib directory only, matching the primary path's VFS. The new test imports `zod`, a real dependency of this repository with real published types, so a leak would resolve rather than merely fail for absence. Three independent layers now stop it; opening any one alone leaves the test passing, and opening all three makes it fail, which is why the test states the property rather than probing one layer.

  **What remains uncovered is interface plumbing.** The fallback's `writeFile`, `getDirectories`, and `realpath` are `ts.CompilerHost` members the compiler never calls under the options we generate. Reaching them would mean invoking the host directly, which would assert that a function returns what it was written to return. Left uncovered deliberately.

  Also pinned: `splitWords` dropping the empty run that doubled or leading separators produce, and `_user` deriving `user` — a caller's visibility convention on one declaration should not become part of every file name.
- [x] T024 [P] Add a determinism guard in `test/unit/determinism-guard.test.ts` plus oxlint rules on the `src/engine/**` override that fail on `Date.now`, `Math.random`, and `process` (`no-restricted-properties` and `no-restricted-globals`, both verified working). `no-restricted-syntax` is unsupported by oxlint, so structural checks — unordered iteration reaching a template, filesystem reads in the generation path — belong in the test half of this task rather than in lint (Principle I, research.md §12). Both rules landed as planned and were confirmed to fire against a planted module: `no-restricted-properties` covers `Date.now`, `Math.random`, `performance.now`, and `process.env`, and `no-restricted-globals` covers the `process` and `performance` globals so that aliasing (`const host = process`) cannot walk around the property rule. `src/engine/verify/**` turns both off, since spawning a compiler and a test runner is its purpose; its determinism is held by the two-engine parity tests instead.

  Two corrections to the plan. oxlint rejects unknown keys in an override, so the exemption's rationale could not live beside it as a `comment` field and is recorded in the test file instead. And no rule was needed to *remove* nondeterminism: the engine had no clock, randomness, or environment read anywhere, so both rules are regression guards rather than fixes.

  The test half carries what lint cannot see. It scans each generation-path module for the same hazards at source level, so the guard survives an edit to the lint config, and asserts none of them reaches the filesystem — including Prettier's `resolveConfig`, which walks upwards for a config file and would make output depend on the directory the server started in. It also asserts the lint rules are still configured, which is the failure mode a source scan alone would miss: a new module added after the rules were quietly dropped. Load-time readers (`catalog/load.ts`, `options/names.ts`) are excluded by name with their reasons, since both read once in sorted order before any request is served.

  Behaviourally it proves the property the source scan cannot: the same request with every object literal's keys reversed resolves to the same options *in the same key order* and hashes to the same bytes, and a partial request hashes identically to the explicit one that resolves to the same values — otherwise the same output would carry two different provenance lines. Removing the lint rules and planting `Date.now()` in `hash.ts` fails two assertions, so both halves bite.

**Checkpoint**: Engine machinery exists and is unit-tested. User story work can begin.

---

## Phase 3: User Story 1 - Request a working pattern implementation (Priority: P1) 🎯 MVP

**Goal**: A caller supplies a pattern name and options and receives a multi-file, compile- and
test-verified bundle, byte-identically on every repeat.

**Independent Test**: Request one pattern with explicit options; confirm the returned files compile,
the returned tests pass, and the response says so. Requires no other story.

### Tests for User Story 1

> Write these first and confirm they fail before implementing.

- [x] T025 [P] [US1] Contract test for the `generate_pattern` happy path via the SDK's in-process client in `test/contract/generate.test.ts`, asserting `diagnosticCount` is 0 and `testOutcome` is `passed`. The client and server share a process over `InMemoryTransport.createLinkedPair()`, driven through `test/contract/client.ts`. Going through the protocol rather than calling the handler is the point: schema validation of arguments, `structuredContent` against the declared `outputSchema`, and `isError` as a result rather than a throw only exist once a request has been serialised.
- [X] T026 [P] [US1] Golden snapshot harness in `test/golden/harness.ts` that stores one expected bundle per pattern × documented option combination (SC-003)
- [x] T027 [P] [US1] Byte-equality harness in `test/determinism/bytes.test.ts` comparing output across repeats **and across process restarts** — a same-process comparison would not catch an ambient dependency (SC-002). Restarts go through `test/determinism/emit.ts`, a separate entry point, since a same-process repeat would be satisfied by any cache. The suite also varies the environment between runs and asserts a differing request still differs, so it cannot be passed by a generator that returns a constant.
- [x] T028 [P] [US1] Refusal tests in `test/contract/refusals.test.ts` for illegal option combinations and invalid identifiers, asserting the message names the field, states the rule, lists alternatives, and returns no code. Covers the refusals reachable through the tool today — unknown pattern, undeclared option, out-of-space value, unusable identifier, and an injected string that must not be echoed. A true `IllegalCombinationError` is not among them because no pattern declares a legality rule yet; that path is held by `test/unit/legality.test.ts` at the engine layer and joins here with `retry` (T035), whose options actually constrain each other. This task also caught a defect: suggestions matched substrings only, so `reslt` returned no suggestion at all, and the fix is `src/engine/catalog/nearest.ts`.
- [x] T029 [P] [US1] Concurrency isolation test in `test/determinism/concurrency.test.ts` proving two simultaneous requests cannot influence each other's output. This one found a real defect rather than confirming a design. The warm compiler's file tree is swapped in place per check, so overlapping requests verified each other's files, and `Typechecker` now serialises checks internally — the guarantee belongs to the shared instance, not to whoever remembers to queue. Serialising costs nothing real: one compiler cannot check two bundles at once, and a queued check still gets the warm instance. The deadline starts when the turn arrives, so a queued request is not charged for the wait. Isolation is also pinned at the verify layer in `test/unit/typecheck.test.ts`, where the shared state actually lives.

### Implementation for User Story 1

- [x] T030 [US1] Define the pattern module interface — template, options, legality, metadata — in `src/engine/patterns/types.ts`. Landed as identity plus `render`, not the four things listed. plan.md's source-tree comment reads as though a module declares its own options, but data-model.md gives `options`, `legality`, `variants`, and metadata to the catalog entry in `data/patterns/{category}.json`, and it is the field-level authority. Declaring them in both places would create two sources of truth that drift silently — the catalog documenting one option set while generation honoured another — and would also defeat the catalog validator, which checks the corpus by parsing JSON rather than by importing generation code. A module therefore names the catalog entry it implements and receives options already resolved against it.

  `RenderContext` is closed on purpose: options, conventions, identifiers, derived names, variant, and nothing else. A template that wanted the clock or the filesystem cannot reach them, which is a stronger guarantee than a lint rule against reaching for them. Derived names are computed once and passed in, so two templates cannot disagree about how the same identifier pluralises.
- [x] T031 [US1] Implement bundle assembly with `File` roles — `core`, `binding`, `adapter`, `test`, `example`, `types` — ordered by role then path in `src/engine/generate/assemble.ts`; never emit in iteration order. `ROLE_ORDER` is `types, core, binding, adapter, example, test`, so a reader meets definitions before uses and tests last. The sequence is asserted by name in the tests because it is part of the output contract: reordering it later would change every bundle's file order and every content hash.

  Sorting is by code unit rather than `localeCompare`, which would order the same paths differently depending on the ICU data the host's Node was built with — the exact class of ambient dependency Principle I excludes. A test pins `B.ts, Z.ts, a.ts` to catch a later switch to locale-aware comparison.

  Scope filtering: `core-only` keeps types, core, example, and tests; `binding-only` keeps the binding and its adapter alone, since the caller already has the machinery and re-emitting it would overwrite a file they may have edited while re-emitting its tests would duplicate a suite they are already running. Assembly also refuses paths a template should never have built — absolute, traversing, drive-lettered, backslashed, directory-named — and refuses two files at one path rather than letting one silently replace the other. All of these are pattern-author defects, so they throw rather than being reported as correctable.

  Reverting the sort fails six assertions.
- [x] T102 [US1] Emit a usage `example` file for every generative pattern, which FR-004 and US1 acceptance scenario 1 both require and which is distinct from the test file. **Half done.** The enforcement landed with T031: assembly refuses any bundle whose scope includes the `example` role but which contains no such file, so the requirement is structural rather than a convention a pattern author has to remember. `binding-only` is exempt, since a binding is a fragment fitted to machinery the caller already has and an example of it in isolation would not run. What remains is the emission itself, which cannot exist before a pattern does — it lands with T034, and this task closes there. **Closed.** The `result` pattern emits `{stem}-example.ts`, separate from `{stem}.test.ts`, and `test/unit/generate.test.ts` asserts the two are distinct files with distinct roles rather than one file doing both jobs.
- [x] T032 [US1] Implement `VerificationRecord` construction in `src/engine/verify/record.ts`, recording compiler version, formatter version, the options actually verified against, and the content hash (FR-006). The builder refuses to construct a record stating something verification did not establish, rather than trusting its caller to only ask when it is true: diagnostics present, `passed` for a bundle with no test files, or `skipped` for a bundle that has them. That middle case is the one worth guarding — a bundle whose tests were never run reporting that they passed is precisely the claim Principle III exists to make unfalsifiable.

  `contentHash` covers each file's path and contents in return order and nothing else. The toolchain is excluded for the same reason it is excluded from the provenance header: folding the compiler version in would change the hash on every release and report drift no file experienced (FR-021). A test pins that a compiler and formatter bump leaves the hash untouched while a reordering of the same files changes it, since files arrive sorted and a different order is a different bundle.
- [x] T103 [US1] Assert in `test/contract/generate.test.ts` that a bundle containing no tests reports `testOutcome: "skipped"` and never a value implying tests passed (spec edge case). Asserted over the protocol, and paired with the absence of any file in the `test` role, so the claim and the bundle cannot disagree.
- [x] T033 [US1] Implement the `generate()` pipeline — resolve, render, format, verify, assemble — in `src/engine/generate/index.ts`. Formatting runs before verification, not after, so what compiles is what is returned. Verification covers the rendered set rather than the assembled one: a `core-only` request is still proven against the tests that exercise it, even though those are not handed back. Two things had to be built to make verification possible at all — declarations for the caller's test runner injected into the compiler's file system, and runtime shims injected into the sandbox — because a hermetic environment has no `node_modules` and `import "vitest"` would otherwise fail to resolve in both places.
- [x] T034 [P] [US1] Author the `result` pattern in `src/engine/patterns/result/` as the reference implementation, with the full method surface a caller needs later (map, mapErr, andThen, unwrapOr, combinators), not a minimal example (Principle IV). Two API decisions were forced by the compiler rather than chosen. `ok` and `err` return the whole union with `never` on the unused side, because a bare `Ok<T>` carries nothing about the error arm and every `andThen` chain would otherwise infer `unknown` and need a hand-written type argument. And every combinator narrows through the `isOk`/`isErr` predicates instead of reading `result.ok`: the discriminant narrows only when `strictNullChecks` is on, so under a `strict: false` project — which Principle IX obliges us to serve — `result.error` becomes an error on the success arm and the module does not compile.
- [x] T112 [US1] **Resolved by amending data-model.md.** The original premise did not hold: §"Shared base options" said all seven are "present on every generative pattern", and authoring a second pattern showed three of them cannot be. The section now specifies a *conditional* base set — a shared vocabulary rather than a checklist, where presence is the pattern's decision and the value space is not — and `PatternSchema` enforces the mechanical half: `includeTests` always; `emitScope` iff `supportsSplit` (both directions); `coreModule` only when splitting; `verbosity` never; and any declared base option must use its documented value space exactly, so `cancellation: "polling"` is refused. Whether a pattern *should* offer `errorMode` stays a judgement the schema does not try to make. A contract test pins the other half of the `verbosity` rule: the same bundle requested at two verbosities hashes identically, since the validator's refusal only protects the hash if the tool layer also keeps verbosity out of the engine request. The reasoning that forced the amendment:
  - `verbosity` must not be a catalog option at all. It selects how the *response* is rendered (FR-028) and changes no generated byte. As a pattern option it would enter `resolvedOptions` and therefore the provenance hash, so the identical bundle would carry a different hash depending on how verbosely it was described. It is already a tool-level input and should stay one.
  - `emitScope` and `coreModule` only mean something for a pattern that separates machinery from bindings. T026 established the invariant — now enforced in `PatternSchema` — that `supportsSplit: false` forbids any other scope, because every value would emit the same bundle. `result` therefore no longer declares `emitScope`, contrary to what this task originally recorded.
  - `errorMode` is contradictory on some patterns and unimplementable on others. On `result` the whole point is that failure is a value, so `errorMode: "throw"` negates the pattern. On `retry` the `result` arm needs a Result type in scope, which is cross-pattern composition — a design question nothing has answered yet.
  `src/mcp/tools/generate.ts` continues to omit fields no pattern declares, rather than advertising inputs that can only be refused; `errorMode` and `async` become tool inputs when a pattern first declares them.
- [x] T035 [P] [US1] Author the `retry` pattern in `src/engine/patterns/retry/` including backoff, jitter, and cancellation plumbing. Options are `backoff` (exponential/linear/constant), `jitter` (full/equal/none), `cancellation` (abort-signal/none), and `includeTests` — 36 combinations, every one of them generated, typechecked, and its emitted tests executed by the golden sweep. The decision that shapes the emitted module is that `sleep` and `random` are both injectable: a retry suite that waits for real backoff is slow and one that draws on `Math.random` is flaky, so without injection the generated tests would violate Principle III no matter how they were written. With it, the schedule is asserted to the millisecond. This task also uncovered a blocker in the verification sandbox — see T113.
- [x] T113 [US1] Declare host facilities to the verification sandbox in `src/engine/verify/platform-types.ts`. Verification compiles with `lib: ["es2022"]` and no package types, which is what makes it hermetic — and also meant `setTimeout` and `AbortSignal` were undeclared, so no bundle that waits or can be cancelled could compile. Since an unverified bundle is never returned, the whole async-resilience category was unreachable; T035 could not be finished without this. The declarations are the portable intersection rather than any one host's, so a timer handle is opaque: Node returns an object and browsers return a number, and generated code that assumed either would compile here and break in half the projects that adopted it. Withheld for `runtime: "browser"`, where `lib.dom` already declares the same names and a second declaration would collide.
- [x] T036 [P] [US1] Author the `circuit-breaker` pattern in `src/engine/patterns/circuit-breaker/` including half-open probe sampling, whose edge cases are exactly what generated tests must pin down. Options are `failureCounting` (consecutive/rolling-window), `halfOpen` (single-probe/sampled), and `includeTests` — 8 combinations, each generated, typechecked, and its emitted suite executed by the golden sweep. Three decisions worth recording:
  - **A class, unlike every pattern so far.** A breaker is nothing but state shared across calls, and expressing it as functions over an explicit state object leaves the caller threading it — which they will get wrong by constructing one breaker per request, defeating the pattern in a way that looks exactly like a working one. The emitted example holds it at module scope for that reason, and says so.
  - **The clock is injected**, the same decision as T035's `sleep`. Every edge of the state machine is a time boundary, so a suite on the real clock either sleeps for the cooldown or asserts nothing about it; with an injected `now` the emitted tests move time and pin the boundary to the millisecond, which is what Principle III asks of a generated suite.
  - **`halfOpen: "sampled"` exists because single-probe recovery is wrong under flapping**: one lucky call re-admits full load, which knocks the dependency over again, and the breaker spends its life cycling. The sampled arm requires a quota of successes out of a bounded number of concurrent probes, and rejects a policy whose quota exceeds its probe limit, since that breaker could never close.
  Also linked `retry` and `circuit-breaker` through `relatedPatterns` in both directions — they are the two halves of one decision, and a caller who found one should be told about the other.
- [x] T114 [US1] Reflow generated comments to the print width, in the format step rather than in each template — `src/engine/format/reflow.ts`, applied after Prettier in `formatSource`, and now asserted for every pattern × combination by the golden suite. Two findings from building it:
  - **It is a caller-facing property, not just a fix for our prose.** `printWidth` is configurable, so a caller asking for 60 was getting 60-column code carrying 80-column comments. The pass wraps to whatever width was requested, which is tested by asserting that a wider limit yields fewer lines — a ceiling-only assertion would pass an implementation that ignored the option.
  - **A comment can belong to no node, and the first version walked past those.** Amended while building `chat-model-port` (T088i): a note alone between a pair of braces — an empty `catch` explaining why nothing is done there — has no statement to lead and no code on its line to trail, so neither of the positions the walk collected from reached it, and it reached the emitted bundle unwrapped at 90 columns. The walk now also scans from just inside any node opening with `{`, which covers an empty block, object literal, class body or `case` block, and the golden suite's width check is what surfaced it.
  - **The parser is required; a scanner is not enough.** `//` in a URL, `/*` in a regex, and either inside a template literal all read as comments to anything treating the file as text, and rewriting one corrupts the bundle. A bare `ts.createScanner` was tried first and demonstrably fails: with no parser driving it a template literal containing a substitution is mis-lexed, and it reported the template's contents as a comment while missing the real ones. Comment ranges now come from a parsed `SourceFile`, and those four lookalikes are the first cases in `test/unit/reflow.test.ts`. Trailing comments are left alone deliberately (wrapping one detaches its tail from the line it annotates), as are fenced examples and tab-indented comments, whose width depends on `tabWidth`.
  Original finding, kept because it is why this lives in the format step: measured on the first three patterns, 34 comment lines in `retry`, 13 in `circuit-breaker`, and 2 in `result` ran between 81 and 172 columns. A pattern composes `dedent` blocks inside other `dedent` blocks, so the column a comment will finally sit at is not knowable where it is written, and wrapping against the wrong column is what produced the 82-column lines `docAt` was added to fix. `sections`, `doc`, `docAt`, and `wrapProse` in `src/engine/render/helpers.ts` stay, but as a convenience that makes a template read like its output rather than as the guarantee.
- [x] T115 [US1] Stop the compiler subprocess outliving its parent. Four `tsc --api` processes were surviving their dead parents and had to be killed by hand, Vitest reported on every run that something was preventing the main process from exiting, and any script calling `generate()` appeared to hang after printing its output. Three parts:

  **`close()` does not end the process.** It ends the child's stdin and drops the reference without waiting — deliberately, since the child blocks on that read and would deadlock on a signal — so what it leaves is a live process nobody is waiting for, reparented to init when the host exits first. `dispose()` now takes the handle *before* closing, since closing discards it, and kills the process afterwards. Reaching past the published surface is justified in the file, and it is best-effort: if the field vanishes upstream the failure mode is what we already had.

  **Abandoned compilers were never released.** `#abandon` detaches a compiler that failed us and schedules its close after a grace period on an unref'd timer, which under a parallel test run routinely outlives the process. They are now tracked and disposal covers them, which is what removed the last stray process.

  **Whoever owns the engine ends it.** A per-file `afterAll` in `test/teardown.ts` for every suite, since forgetting is silent — the suite that forgot still passes and the cost lands on whichever file runs last — and `createServer` wraps `close` so a host that shuts its server down can exit. The setup file imports the engine dynamically, and that is not a style choice: a static import loads the whole pattern graph before `vi.mock` can replace anything, which silently defeated six suites that mock a pattern module.

  **Unreffing was the other obvious answer and is deliberately not used.** It works, and it also lets a host exit *during* a check — a script generating one bundle exited mid-verification with the promise unsettled. Trading a hang for a silently dropped verification is not an improvement. Side effect of the fix: the unit suite went from 29s to 9s and the whole gate from 43s to 20s, because runs no longer wait out a 10s close timeout.
- [x] T037 [US1] Export the engine public API from `src/index.ts` per contracts/engine-api.md, with no MCP imports. `listPatterns` and `describePattern` are absent rather than stubbed, since they arrive with T046 and T047 and a placeholder is something a caller can bind to. `toolchain()` is also deferred: it reports a catalog version, and nothing yet defines what that version is.
- [x] T038 [US1] Construct the MCP server in `src/mcp/server.ts` with read-only annotations and cache hints per contracts/mcp-tools.md. All four annotations are true of generation rather than merely asserted about it — it writes nothing and consults nothing outside the request — and the cache hint is a consequence of Principle I: an identical request cannot produce a different bundle, so a cached response cannot be stale.
- [x] T039 [US1] Register the `generate_pattern` tool in `src/mcp/tools/generate.ts`, deriving `inputSchema` and `outputSchema` from the Zod schemas. `pattern` is a described string rather than the closed enum the contract asks for, and that is a deliberate trade recorded in the file: an enum is validated by the SDK before the handler runs, which would replace "no pattern named X, did you mean Y" with a schema violation and lose the nearest-match suggestion the same contract requires. It becomes the better trade once the catalog is public API (T046) and the description can enumerate the names.
- [x] T040 [US1] Map engine errors to `isError: true` results in `src/mcp/errors.ts`, escaping or eliding every caller-supplied value before it appears in a message (FR-035) — tool handlers in SDK v2 return results, not protocol errors. Messages are rebuilt from each error's structured fields rather than forwarded from `error.message`: the engine quotes the values it rejects, which is right for a library whose caller is a program and wrong here, where the message may be pasted into another prompt. A caller value is quoted back only when it is inert — identifier-shaped, no whitespace, length-capped — and described rather than echoed otherwise, since prose is what carries an instruction.
- [X] T041 [US1] Implement the stdio transport entry point in `src/mcp/transports/stdio.ts`, routing all diagnostics to `stderr`. `serveStdioOn` takes its transport and log sink as arguments so `test/contract/stdio.test.ts` can drive real wire frames over injected streams; `main` owns the process and is the only half allowed to touch globals, where it also points `console.log`/`info`/`debug`/`trace` at stderr — the risk on this transport is not our own stray write but a dependency's, and a single one desynchronises the client. `stdio-bin.ts` is separate so that importing the transport does not seize this process's stdin. Verified that a malformed frame is reported out of band to the log sink while an unknown method is answered in band, which is the split that matters: diagnostics to stderr, protocol replies to stdout.

  **This task exposed a defect that made the published package entirely non-functional.** `data/patterns/` and `data/names.json` were located by a fixed relative path from their own module (`../../../data/...`), correct in the source tree and three levels above the package root once the bundler flattened `src/engine/catalog/` and `src/engine/options/` into one directory. Every request through the built binary failed on a missing file outside the installation. The whole gate passed throughout, because every stage before this one ran against `src/`. Fixed by `src/engine/data-root.ts`, which walks up for a directory holding both `package.json` and `data/` — shared, because two copies of one relative path is what produced two instances of the bug — and by adding `data` to the published `files`, without which no path would have helped. Pinned by `pnpm smoke` (`scripts/smoke-packaged.ts`), now the last stage of the gate: it launches the built binary from a temporary directory and requires a verified bundle, a stdout carrying nothing but protocol frames, and a silent stderr. Mutation-verified by moving `data/` aside.
- [X] T042 [US1] Implement the verification-failure path: throw `VerificationError` with a correlation identifier and never return an unverified bundle (Principle III, spec edge case). The throw sites existed from T033; what this task added is the evidence and the other half of FR-038. `test/unit/verification-failure.test.ts` replaces the pattern module with one that renders code failing to compile, then tests that fail at runtime, and asserts the refusal blames itself, quotes a correlation identifier that is stable for a given request, and carries no compiler code or path in the caller-facing message while keeping the diagnostics on the error object. `src/mcp/errors.ts` now records those diagnostics to stderr against that identifier: an identifier leading nowhere is worse than none, since it invites a bug report the operator cannot look up. Correctable refusals are deliberately not logged — they are the caller's business, fully described in what they already received, and logging every typo would bury the real failures. Also widened the `node:assert/strict` shim, which declared `strictEqual` but not `equal`; in that module they are the same function, so an ordinary `assert.equal` failed to compile and read as a broken pattern rather than a missing declaration.

**Checkpoint**: The product's core promise works end to end and is independently verifiable. This is the MVP.

---

## Phase 4: User Story 2 - Discover what is available and how to configure it (Priority: P2)

**Goal**: An agent with no prior knowledge can browse the catalog and learn one pattern's options well
enough to construct a valid request on the first attempt.

**Independent Test**: Ask for the catalog, pick a pattern, ask for its details, and confirm the option
names and values returned are sufficient to build a successful generation request.

### Tests for User Story 2

- [x] T043 [P] [US2] Contract tests for `list_patterns` and `describe_pattern` in `test/contract/discovery.test.ts`, including every filter combination — all 48 of them (7 categories + unfiltered, × 2 kinds + unfiltered, × 3 tiers + unfiltered), each cross-checked against the same filter applied to the loaded catalog. Exhaustive rather than representative because a filter's failure mode is silence: one that matches nothing looks exactly like a correct empty answer, so every combination is checked both for what it returns and for what it omits. The suite also asserts the negative half of FR-027 — a summary carries seven fields and the word `affects` appears nowhere in it — and that an unknown name is refused with suggestions while a name that reads as prose is not echoed back.
  Unit-level coverage sits alongside it in `test/unit/catalog-discovery.test.ts`, for the two shapes the shipped catalog does not contain: no pattern is advisory, and none has a legality rule, so both branches would otherwise first run in the release that adds such an entry.
- [x] T044 [P] [US2] Drift test in `test/contract/catalog-parity.test.ts` asserting the resource payload and the tool output are generated from the same catalog data and cannot diverge. Made structural before it was asserted: `listing()` and `detail()` in the tool modules are the single path, and the resource handlers serialise their return value, so there is no second place where a summary's fields or a rule's wording could be decided. The test compares **bytes** — the resource text against `JSON.stringify(structuredContent, null, 2)` — for the catalog and for every pattern, rather than deep-equal fields: deep equality would pass on two payloads that two code paths happen to agree on today, which is the thing being ruled out.

### Implementation for User Story 2

- [x] T045 [US2] Author tier-1 catalog entries for the three US1 patterns in `data/patterns/type-safety.json` and `data/patterns/async-resilience.json` — already satisfied: `result`, `retry`, and `circuit-breaker` were authored with their patterns in T029/T035/T036, all tier 1, all validated by `pnpm catalog:check`. Nothing to add; recorded rather than silently skipped so the phase's inventory is honest.
- [x] T046 [US2] Implement `listPatterns` with `category`, `kind`, and `tier` filters in `src/engine/catalog/list.ts`, returning summary fields only so the payload stays cheap (FR-027). A summary is a different answer, not an abbreviated one: `intent` and `supportsSplit` answer "which of these do I want", and the option documentation that answers "how do I call this one" is what would make browsing cost proportional to the size of the whole catalog. Filters are conjunctive and each optional, so no combination is an error — a caller asking for a tier nothing occupies has asked a valid question with an empty answer. An advisory entry reports `supportsSplit: false` rather than omitting the field, since a caller sorting on it should not have to handle an absent value for a property whose answer is known.
  Two shared-vocabulary decisions came out of this. The kind and tier value spaces are now `PatternKindSchema`/`TierSchema` in `catalog/schema.ts` and reused by the tool filters, because a filter that re-declared them could accept a value the catalog can never hold — a request that is valid, answerable, and permanently empty. And the process-wide catalog cache moved out of `generate/index.ts` into `catalog/load.ts` as `catalogOnce()`: discovery and generation must agree about what the catalog contains, and two caches of one file are two chances to disagree.
  This also settles T037's deferral: `listPatterns` and `describePattern` are now exported from `src/index.ts` with the catalog bound, and the MCP tools consume *those* rather than reaching into the catalog themselves — which makes the adapter a genuine consumer of the contract and keeps the resource, the tool, and any future CLI on one path. Both are `async` where contracts/engine-api.md shows them synchronous, because the catalog is read from disk and the only way to keep the documented signature is a synchronous read, which means a second loader and therefore the second cache this task just removed.
- [x] T047 [US2] Implement `describePattern` in `src/engine/catalog/describe.ts`, returning every option with values, default, description, and the legality rule text verbatim. Legality is returned as its prose *and* its `when`/`forbids` predicate — the prose is what a caller reads, the predicate is what a caller can evaluate before calling, which is the difference between documentation and a rule that can be checked. An unknown name throws `UnknownPatternError` rather than returning an empty detail, because a caller who cannot distinguish "no such pattern" from "a pattern with nothing to say" will write the second case's handling for the first. Advisory entries come back with empty collections rather than absent ones, so iterating options is safe without first branching on `kind`.
- [x] T048 [P] [US2] Register the `list_patterns` tool in `src/mcp/tools/list.ts`. An empty result renders as a sentence saying so: rendered as an empty string it would be indistinguishable from a failure or a lost response, and this is the one tool whose correct answer is routinely nothing.
- [x] T049 [P] [US2] Register the `describe_pattern` tool in `src/mcp/tools/describe.ts` with `pattern` as a closed enum — **not done as specified, deliberately.** The contract asks for both a closed enum of catalog names *and* an `isError` result naming the nearest entries for an unknown pattern, and those cannot both hold: an enum is checked before the handler runs, so the suggestion is unreachable. The rest of the contract settles which to keep — it classifies an unknown pattern as a result error rather than a protocol error, which only makes sense if the handler sees the value. Two further reasons point the same way: the enum would copy the catalog into the schema of every tool that names a pattern, which is the payload cost `list_patterns` exists to avoid (FR-027), and it would grow with every release, churning a schema advertised as cacheable. So `pattern` is a described string, consistently with `generate_pattern` (T039), and the reasoning is recorded in the module rather than left as a silent divergence. This supersedes T039's note that the enum becomes the better trade once the catalog is public API.
  Options are described as a union discriminated on `type`, mirroring the catalog, so `values` exists only where there is a closed set — a flat shape with everything optional would let a caller read a `values` array off a boolean option and reason about a value space that does not exist.
- [x] T050 [US2] Register `pattern://catalog` and the `pattern://catalog/{name}` resource template in `src/mcp/resources/catalog.ts`, marked cacheable for the package version's lifetime (FR-014). Both route through the tools' own `listing`/`detail`, which is what makes T044 a test of the arrangement rather than of the data. The template supplies a `list` callback and name completion: a template without one is discoverable only by a client that already knows a pattern name, which is exactly the knowledge a caller starting out lacks. An unknown name fails with the SDK's `ResourceNotFoundError`, whose message goes through the same sanitiser as the tool refusals — `safeMessage` was extracted from `src/mcp/errors.ts` for that, so FR-035 belongs to the adapter rather than to the tool path alone. The URI itself is echoed, because the protocol's not-found data field is for exactly that and a client matching a response to its request needs it; the message is where prose could be read as an instruction.
  "For the package version's lifetime" had to become a number, since the revision's hint is `ttlMs`. A day, with `cacheScope: "public"`: the data cannot change without a new build, so no TTL is wrong on the data's own terms, and the only failure a longer one enables is an upgraded deployment serving a client that still holds yesterday's catalog — a remote-transport concern, since over stdio a new version is a new process.
- [x] T051 [US2] Declare `server/discover` capabilities in `src/mcp/server.ts` — `listChanged: false`, no `logging`, no sampling, no roots — with short `instructions` stating the list → describe → generate order. `listChanged: false` is a fact about the design rather than an unimplemented feature: the catalog ships with the build and generation is a pure function of its input, so nothing this server can answer changes while it runs. `instructions` is kept to two sentences because it is prepended to a caller's context every session — the call order, and the one guarantee that changes how the output should be treated, namely that it has already been compiled and tested.
  **Finding: tool results cannot carry a protocol cache hint on this revision.** The contract says `list_patterns` and `describe_pattern` are cacheable, but the revision's cacheable-result list is closed and `tools/call` is not on it — it covers the `list` operations, `resources/read`, and `server/discover`. So the tools keep an advisory `_meta` hint (as `generate_pattern` already did) and the protocol-level hints are declared per operation in `ServerOptions.cacheHints`, which is where the SDK actually honours them.
- [x] T052 [US2] Implement nearest-match suggestions for unknown pattern names in `src/engine/catalog/suggest.ts` (spec edge case) — implemented in T029 as `src/engine/catalog/nearest.ts`, and now used by `describePattern` as well as by generation, which is what the module was placed beside the catalog for: the same mistake has to earn the same suggestion from both, and two implementations would eventually disagree about what is near.

**Checkpoint**: US1 and US2 both work independently.

---

## Phase 5: User Story 3 - Reuse an installed pattern cheaply (Priority: P3)

**Goal**: Applying a pattern to a second domain type returns only the binding, importing machinery the
caller already has.

**Independent Test**: Generate a pattern in full, then request it for a different domain type while
pointing at the first result's module; confirm the second response contains only binding files that
import from that location.

### Tests for User Story 3

- [x] T053 [P] [US3] Reuse-economy test in `test/contract/reuse.test.ts` asserting a binding-only response is at most 20% of the full response's content (SC-004). Measured in characters of content rather than in files, which is the cost a caller actually bears — tokens through a model, bytes over a transport — where a file count would report a saving of two-thirds for a bundle that shrank by nothing. `repository` comes in at about 11%.

  A second case compares against a full bundle with `includeTests: false`, because the ratio alone would hide the difference between saving the machinery and merely dropping a suite. Only the first is reuse; the second buys a caller nothing. The measured ratio is attached to the assertion message, since a threshold nearly breached and one met by an order of magnitude are otherwise the same green tick.
- [x] T054 [P] [US3] Diff-stability harness in `test/determinism/diff-stability.test.ts` asserting, for every option of every pattern, that changes stay confined to the surfaces named in that option's `affects` field — a reflowed unrelated function fails even though it still compiles (SC-005). Building it forced two decisions and immediately found eleven understated declarations:

  **`affects` is now a closed vocabulary.** It was `z.array(z.string())`, which cannot be checked: an option declaring `affects: ["behaviour"]` names nothing the harness can look for, so the claim passes by being unfalsifiable. The values are now the file roles plus `files`, which is the vocabulary `emitScope` and ordering already use, so it is not a new concept for a reader — and `data/schema.json` carries the enum too, so a catalog authored against the JSON Schema is checked before it reaches Zod.

  **Role granularity is not enough on its own, so the blast radius is recorded as well.** A pattern's core is one file, so an option licensed to change anything in the core is licensed to change everything in it, and "changing `jitter` rewrote the whole retry loop" would pass the role check. The task's own wording — "a reflowed unrelated function fails even though it still compiles" — needs a finer grain than a file. `affects` cannot supply it, because declaration names depend on the identifiers a caller asks for (`OrderRetryPolicy`, not `RetryPolicy`), so a pattern cannot state them in advance. So the harness parses both bundles, reduces each to a map from top-level declaration to its text, and stores the set an option moves as a snapshot — the golden-bundle bargain applied to option deltas. Statements with no declared name are labelled by their call title, `describe("the delay schedule")`, because an index-based label renumbers every suite below an insertion and reports code the option never touched.

  **What it found.** Every option that reshapes a core also reshapes the tests exercising it, and no pattern said so: eleven violations across all three patterns. `retry`'s `cancellation` also rewrites the example. `includeTests` was declared `["files"]`, which is true and incomplete — removing the test file changes the `test` surface as well, and a role is credited for a file appearing or vanishing precisely so that a pattern cannot evade the role claim by renaming. All eleven declarations are now the truth rather than the intention.

  **Limitation, deliberately.** Only single-option deviations from the default are swept, matching FR-010's own wording. An option pair that interacts only away from the defaults is not covered; the golden suite stores the full cartesian product, and doing the same here would multiply an already-multiplicative cost.

  **Interaction with T057.** The provenance header carries the options hash, so *every* option changes it. Left in, the harness would report every option as affecting every file and would have nothing to say about the code — hence `withoutHeader`, and hence its own test that it strips only a header this generator wrote rather than any leading block comment.

### Implementation for User Story 3

- [x] T055 [US3] Implement `emitScope` filtering by `File` role in `src/engine/generate/assemble.ts` (FR-017). Role filtering landed with T031; what this added is the invariant that makes filtering safe, and it found a shipped bug immediately.

  **A scope must select whole subsets, not cut files loose from their imports.** `core-only` was emitting an example and a suite that imported `./order-repository.js` — a binding the scope drops. Both halves were individually correct, and no existing check could see it: verification typechecks the *rendered* set on purpose, so the import resolved there and failed only once the caller had the files. `assertSelfContained` now refuses any bundle whose emitted files import a sibling the scope did not keep, which is a pattern-author or scope-table defect and so throws. Bare specifiers are exempt because they name what a caller installs, and `coreModule` is exempt whichever form it takes — a relative one names a file they already have, and without the exemption every relative `coreModule` would be refused.

  **The fix to `core-only` itself.** The scope keeps its example and suite, and the `repository` pattern now renders them differently there: with no binding file to import, both declare a sample binding inline from the same generator the binding file uses. That is better content than the alternatives — dropping them leaves a core-only caller with no example at all, and FR-004 asks for one — since what a caller adopting the machinery does next is write exactly that binding.
- [x] T056 [US3] Implement the `coreModule` requirement and import rewriting for `binding-only` in `src/engine/generate/imports.ts` (FR-018). Templates always import their siblings and the engine repoints them afterwards, so a template cannot forget: one that did would emit a binding importing a file the caller does not have, and it would still typecheck, because verification synthesises the core. Every rendered file is rewritten rather than only the emitted ones, since the whole rendered set is what gets checked. Specifiers are located with the parser rather than by substitution — a textual replace would also rewrite the same string inside a template literal or a doc comment, which is how a "fix" to an import corrupts prose.

  Also added `importsFrom`, because three patterns were each hand-rolling the `typeImports` split as a ternary plus a trailing `when(!separateTypes, …)` — six chances per file to emit an `import type` to a project that spells it inline. Templates now say which names they need and not how the caller writes them.
- [x] T104 [US3] Implement stub synthesis for binding-only verification in `src/engine/verify/synthesize-core.ts` per research.md §11: regenerate the core into the verification file system at the `coreModule` specifier, typecheck the binding against it, then discard it from the emitted bundle.

  **Nothing is rewritten to make it resolve.** The specifier in the verified bytes is the specifier in the emitted bytes, which is what keeps "the code that was checked is the code you received" true for a fragment as well as for a whole bundle. A relative specifier has the core written at the path it resolves to; a bare one has it written as a package under `node_modules`, which is the device the test-runner shim already uses to make `import … from "vitest"` resolve in a sandbox with no dependencies. Repointing the specifier for verification would have been simpler and would have cost the invariant.

  **An ambient `declare module` cannot do this.** It was the first attempt: TypeScript refuses to let one re-export through a relative path (TS2439), so the declaration would have to restate the core's entire surface — a second copy, free to disagree with the first, which is how verification comes to pass because the declaration was wrong rather than because the code was right.

  **The `package.json` needed its own channel.** It is not TypeScript, and the sandbox transpiles everything it writes; transpiling JSON yields a syntactically valid JavaScript expression and a `package.json` that no longer parses. `runGeneratedTests` now takes `verbatimFiles` for exactly this.

  **`testOutcome` had to stop describing the bundle.** The record's invariant was "passed requires a test file in the bundle", which is false here: a binding-only bundle emits no suite and is still executed against one. The invariant is now against what verification *ran*, so the evidence is neither overstated nor — as it was, briefly — understated as `skipped`.
- [x] T105 [US3] Refuse a binding-only request whose regenerated core does not match what the pattern expects, explaining what was expected rather than returning code that cannot compile (spec edge case). **Delivered as a note, not a refusal, because the mismatch is undetectable here.** The core regenerated for verification is the one *this request's* options describe; the file in the caller's repository is invisible to us. A binding built for cursor paging and a core installed with offset paging are each internally consistent, so there is no diagnostic to raise — only an expectation to state. So the bundle carries a note naming the options the core must have been generated with, derived from `affects` rather than hand-written so it stays true when an option is added, and a caller compares it against the `@options` line in their installed core. That turns the failure from a compiler error in a file they did not generate into a glance at their own provenance header.
- [x] T057 [US3] Implement provenance header emission in `src/engine/provenance/header.ts` carrying pattern identity and `optionsHash` only — embedding a generator version would rewrite every generated file on every release and destroy diff-stability (FR-020, FR-021). Emitted between rendering and formatting, which is what makes the verified bytes the returned bytes: a header prepended after verification would mean returning a file in a form nothing had checked. Three things the format decided:

  **Tag per line, not prose.** The format step wraps comments to the caller's `printWidth` (T114), so a header written as a sentence is re-wrapped at a narrow width and its fields run together — machine-readable until someone asks for 60 columns. Every line is a `@tag`, which the wrapper treats as its own paragraph, and a test pins the fields to separate lines at a width that forces a rewrap.

  **`@generated` first.** A recognised convention rather than a private one, so review tools and diff viewers get the signal without being taught this format.

  **In the contents, not beside them.** data-model.md lists `provenance` as a field of `File`; it is modelled as the header the contents carry rather than a second copy alongside them, because the point of provenance is surviving being pasted into a repository — which a response field does not do — and two copies of one fact can disagree.

  Also replaced the placeholder correlation identifier on `VerificationError`, which was `pattern:optionCount`, with the request's own options hash. An operator reading it off a caller's error can now regenerate exactly the bundle that failed.
- [x] T058 [US3] Refuse `binding-only` for patterns whose catalog entry has `supportsSplit: false`, explaining what that pattern does support (US3 acceptance scenario 3). A distinct `SplitUnsupportedError` rather than the generic unknown-option refusal, because the two are different questions: "you misspelled an option" and "this pattern is one module, so there is no second half to ask for" lead a caller to different next actions.
- [x] T059 [US3] Author the `repository` pattern with a genuine core/binding split in `src/engine/patterns/repository/`, plus its catalog entry in `data/patterns/data-access.json`. The core is a query language, a repository over an abstract `Store`, and an in-memory `Store` complete enough to develop against; the binding is one entity's key type, collection, and factory. Five things this pattern forced:

  **The core is deliberately one file.** A binding-only request repoints its imports at the one module the caller has, and two core files would mean two specifiers to repoint from one `coreModule` value — either the caller supplies two, or we guess. `synthesizeCore` refuses more than one for the same reason, stated where it would otherwise be inferred.

  **`expect().rejects` had to exist.** Every method returns a promise and three of the properties worth asserting are refusals, so the suite is mostly rejection assertions. Added to the runtime shim, the ambient declaration, and the emitted `expect.ts` together, since a matcher present in two of the three is worse than absent from all three. `toThrow` is excluded from the rejection context, where it would be nonsense.

  **Doc comments were becoming detached.** `sections()` puts a blank line between its parts, which is right for declarations and wrong for a comment and the thing it documents — the JSDoc floats free and the editor shows nothing on hover. Hence `documented`/`documentedAt`, which join the two.

  **Indefinite articles cannot be interpolated.** "A `Order`" appeared in generated prose, and choosing the article from the identifier is a guess English does not reward. The prose is reworded to sidestep it — "The key type for `Order`" — and the rule is written down in `render/helpers.ts` where the next author will meet it.

  **`toSorted` is ES2023.** The sandbox targets ES2022, so the generated sort is `[...records].sort()`, which is non-mutating for the same reason and compiles for every caller.
- [x] T060 [P] [US3] Add golden snapshots covering `full`, `core-only`, and `binding-only` for every split-capable pattern. The cartesian product already covered `full` and `core-only`, and could not reach `binding-only` at all: `coreModule` is a string option, so it contributes its default alone, and its default is empty — which `binding-only` refuses. Every `binding-only` case was therefore a snapshot of that refusal. `splitCombinations` adds the two the product cannot express, one specifier of each kind, because a relative and a bare `coreModule` take different routes through verification and are otherwise the same bundle apart from one string.

**Checkpoint**: Reuse compounds. US1–US3 all work independently.

---

## Phase 6: User Story 4 - Be steered away from the wrong pattern (Priority: P4)

**Goal**: Requesting a pattern TypeScript has made unnecessary returns idiomatic advice, as a success.

**Independent Test**: Request a known-obsolete pattern; confirm the response is advisory guidance with
an alternative and rationale, marked as advice, and not an error.

### Tests for User Story 4

- [ ] T061 [P] [US4] Advisory contract test in `test/contract/advisory.test.ts` asserting a success result with `kind: "advisory"`, a named alternative, a rationale, and no generated class hierarchy

### Implementation for User Story 4

- [ ] T062 [US4] Short-circuit advisory patterns in `src/engine/generate/index.ts` before option validation, per the validation order in data-model.md (FR-022)
- [ ] T063 [US4] Author advisory catalog entries — starting with `singleton` — across `data/patterns/*.json`, each with `alternative` and `rationale`
- [ ] T064 [US4] Extend `scripts/validate-catalog.ts` to enforce the advisory invariants: `kind: "advisory"` implies empty `options` and a present `advisory` block
- [ ] T065 [P] [US4] Surface `kind` in `list_patterns` output so an agent can skip the call entirely (FR-023)

**Checkpoint**: A category of actively harmful output is now impossible to request successfully.

---

## Phase 7: User Story 5 - Match the calling project's conventions (Priority: P4)

**Goal**: Output follows the caller's house style and is verified under the caller's own settings.

**Independent Test**: Submit the same request with two different convention sets; confirm each result
conforms to, and was verified against, the conventions supplied with it.

### Tests for User Story 5

- [x] T066 [P] [US5] Conventions contract test asserting `verification.compilerOptions` reflects the caller's options, not ours. Landed in `test/contract/generate.test.ts` rather than a file of its own, beside the other assertions about what evidence reaches a caller; the convention *sweep* is `test/conformance/conventions.test.ts`, which crosses every axis with adversarial names, and that suite existed for some time while this assertion did not. Worth separating the two, because they are different claims: the sweep proves a bundle generated under a convention set compiles, and this proves the record hands back the options it was checked under. Without it the mapping was unit-tested and its result never observed through the protocol — a record naming our own defaults would have read identically to a caller whose project is `strictest`, and proved nothing about their build
- [x] T067 [P] [US5] Convention fixtures in `test/fixtures/conventions/`. Three, and each is read by something: `cjs-loose.json` by quickstart scenario 9 and by the parity suite, `esm-strictest.json` by a second parity case, `unconfigurable.json` by the parity refusal case. The two settings files were written to disagree on every field, which is what makes them worth having as a pair — a surface that read the file and then defaulted a field would match on loose CJS and diverge on strictest ESM, and one fixture would have proved only that the path resolves

### Implementation for User Story 5

- [x] T068 [US5] Map `Conventions` onto compiler options — this mapping *is* the verification configuration, not decoration (FR-025). Lives in `src/engine/verify/typecheck.ts` as `compilerOptionsFor`, not the `src/engine/options/compiler-options.ts` this task named, and the difference is the point rather than an accident: the mapping and the checker that consumes it are one concern, and a module between them would be a place for the two to disagree about what `strictest` means. Unit-tested per axis in `typecheck.test.ts`
- [x] T069 [US5] Apply module style, import extensions, and type-import style in the render layer. Split across `src/engine/generate/imports.ts`, which writes the specifier and chooses between an inline and a separate type import, and `src/engine/render/helpers.ts`, which resolves a sibling's specifier under the caller's extension convention
- [x] T070 [US5] Detect internally contradictory conventions and refuse, naming the conflict (spec edge case). `assertCoherent` in `src/engine/options/conventions.ts`, with `ContradictoryConventionsError` — a class of its own rather than `IllegalCombinationError`, because that one is about a pattern's declared rules and would send a caller off to try a different pattern for something that holds across all of them. One pair listed: `runtime: "browser"` with `testFramework: "node-test"`, which was being served as four files, zero diagnostics and "tests passed" — true here, where the suite runs under Node, and false in the browser the caller named. The refusal names both settings and a way out of each, because there is no fact about which one they meant. Deliberately a short list: a pair belongs here only where honouring both is impossible, not where it is unusual, since refusing taste is how a generator becomes something callers work around. The conventions sweep did not catch it and structurally could not — it varies one axis at a time from the defaults, so the two halves were each tried against the other's default and the pair was never formed
- [x] T071 [US5] Verify under caller-supplied options and report the fully resolved conventions, including defaults, in the response (FR-007, FR-026). `resolvedConventions` on the bundle and `compilerOptions` on the verification record, both asserted through the protocol by T066
- [x] T072 [US5] Wire the caller's Prettier configuration into the format step in `src/engine/format/prettier.ts` (FR-024). Allowlisted rather than passed through, so a caller cannot turn a style setting into a semantic one, with a stated floor on print width (FR-049) below which directives cannot be kept attached to the lines they assert about

**Checkpoint**: Output arrives committable rather than needing cleanup.

---

## Phase 8: User Story 6 - Use the generator without an agent (Priority: P5)

**Goal**: The same generation from a terminal or script, byte-identical to the MCP surface.

**Independent Test**: Generate the same pattern and options through both surfaces and confirm
byte-identical results.

### Tests for User Story 6

- [ ] T073 [P] [US6] Parity test in `test/parity/surfaces.test.ts` comparing CLI `--json` output against MCP `structuredContent` byte-for-byte across a request matrix; divergence means generation logic leaked into an adapter (SC-010)

### Implementation for User Story 6

- [ ] T074 [US6] Implement `list`, `describe`, and `generate` argument parsing in `src/cli/index.ts` per contracts/cli.md, with one flag per tool field using identical names and values
- [ ] T075 [US6] Implement `--json` output mode emitting the `GenerateResult` structure verbatim on `stdout`
- [ ] T076 [US6] Implement file writing with `--out` and `--dry-run`, erroring on collision rather than overwriting silently
- [ ] T077 [US6] Implement exit codes 0, 1, 2, and 70, keeping `70` distinct because it means our defect rather than the caller's
- [ ] T078 [US6] Discover conventions from `tsconfig.json` and Prettier config in the **CLI adapter only**, passing explicit values inward so `generate()` stays pure
- [ ] T079 [US6] Add the `bin` entry to `package.json` and confirm the built binary runs from a clean install
- [ ] T080 [P] [US6] Document CLI usage in `README.md`

**Checkpoint**: All six user stories work independently. Delivery risk from MCP-only distribution is hedged.

---

## Phase 9: Polish & Cross-Cutting Concerns

- [ ] T081 Implement the stateless Streamable HTTP transport in `src/mcp/transports/http.ts`, constructing a fresh server instance per request (FR-030, FR-031)
- [ ] T082 Resolve the open item on header validation: send a deliberately mismatched `Mcp-Method` and observe whether the SDK rejects it; if not, implement validation of `Mcp-Method`, `Mcp-Name`, and `MCP-Protocol-Version` against body values returning HTTP `400` with `-32020`. Assuming SDK coverage without testing it would be a conformance failure
- [ ] T106 Implement the remaining transport security requirements in `src/mcp/transports/http.ts`: validate `Origin` and `Host`, answer legacy `GET` and `DELETE` with `405`, and never mint, echo, or read a session identifier (FR-037). These are constitution MUSTs that the conformance suite alone will not necessarily catch
- [x] T107 [P] Confirm external JSON Schema `$ref` resolution is disabled in the server's validator configuration and add a regression test in `test/contract/schema-refs.test.ts` (FR-039). Nothing needed fixing, which is worth recording along with why. Zod inlines every subschema, so the published documents contain no `$ref`, `$defs` or `definitions` at all — there is no reference for a client to follow and therefore nothing for a resolver to fetch. The one remote URL in them is `$schema`, the dialect declaration, which is compared as an identifier rather than retrieved; the test excludes it by name rather than by pattern, because matching it as a reference would fail on a correct schema and a test that fails on correct output gets switched off. The inbound half is asserted separately: `$ref`, `$id` and `file:` targets sent inside `prettierConfig`, `options` and `identifiers` — the open records, where a caller's own JSON arrives — are answered without a `fetch`, asserted by making `fetch` throw for the duration of the call. Both halves matter because they fail in opposite directions, and the second is the one a change of validator would quietly break
- [x] T108 [P] Publish explicit cacheability on every result rather than relying on the SDK's conservative defaults, and assert it in `test/contract/cache-hints.test.ts` (FR-042). Writing the assertion found that the statement was going to two places and reaching neither of the ones that needed it. The protocol's own cache fields were declared for the closed list of cacheable results and are correct, but the pinned SDK negotiates no further than `2025-11-25` and the fields are defined by `2026-07-28`, so nothing carries them today; a probe against the wire shows silence, which is indistinguishable from a declaration that never worked. Meanwhile `tools/call` is *permanently* off that closed list, so the three tools that do the work can never use the protocol fields — and their `_meta` annotation sat on the tool descriptors only, which are read once at discovery and describe a capability rather than the answer in hand. So a caller holding a generated bundle was told nothing about it and had to assume the SDK's fallback, `ttlMs: 0` and `cacheScope: private` — a description of a server whose answers vary by caller and go stale immediately, which is the opposite of this one. Fixed by putting the hint on every tool result, refusals included, since the same bad request is refused identically and a caller who does not know that retries a call whose outcome is fixed. The one result that deliberately carries no hint is the unclassified internal failure: a defect is not a fact about the request, and caching it would make one bad moment permanent. Also consolidated two spellings of one concept — `{ cacheable, scope }` in the server and `{ ttlMs, cacheScope }` in the resources — onto the SDK's `CacheHint` shape in `src/mcp/cache.ts`, because the annotation uses the protocol's reserved key and a client that understands the key should not need a second spelling of its fields. The dormant half is asserted as data rather than through the wire, and the suite carries a case that takes over automatically once `SUPPORTED_PROTOCOL_VERSIONS` contains the revision, so the upgrade is what activates the wire assertion rather than someone remembering to. **Two claims in this entry were false when written, corrected under T147**: the protocol fields were not dormant — they had been reaching every modern client from the start, and the constant this task chose as the trigger enumerates the *legacy* revisions, so the condition it waits for can never come true and the case standing in for it asserted the fields could not work. And the key was not the protocol's to reuse: `io.modelcontextprotocol/` is reserved by structure for the specification and its official extensions, which define nothing called `cache-hint`, so the reasoning about sparing a client a second spelling was buying a familiarity no client had
- [x] T109 [P] Assert in `test/contract/error-hygiene.test.ts` that no response ever contains compiler output, a stack trace, or a filesystem path, and that internal failures carry only a message plus a correlation identifier (FR-038). The withholding half was already right everywhere the test looked — eight shapes of refusal, a substituted verification failure carrying sandbox paths and `TS2769` in its diagnostics, and a successful bundle checked whole. Two defects sat on either side of it. **A failure nothing classified carried no correlation identifier.** `VerificationError` mints one and quotes it, but the branch for a defect that escaped a boundary logged the stack and returned prose, so the caller most likely to report a problem — the one whose message tells them nothing else — had nothing to reference, and the operator had a log line nobody could point at. FR-038 does not exempt that branch. Closed by deriving the identifier from the error's own identity, following the reasoning already recorded for the verification one: an arbitrary identifier is unique and useless, while a derived one is reproducible, so two reports of one defect arrive under the same reference. The stack is deliberately excluded from what is hashed, since including it would move the identifier whenever a line number did — precisely when two reports most need to agree. **And a caller's error was being reported as ours.** `FormatConfigError` was declared beside the Prettier allowlist that throws it, extending plain `Error` rather than `CorrectableError`, so it fell through every adapter's classification: a caller who mistyped a formatter option was told the server had a defect and invited to report it, while the message that would have fixed their call in one round trip — the option named, the permitted ones enumerated — was discarded. Moved into the taxonomy in `src/engine/errors.ts`, which is the file adapters branch on and the reason an error class defined outside it is one nothing thinks to classify, with the permitted list passed in so the taxonomy need not import the formatter. The MCP adapter gets an explicit branch rather than falling through to the generic correctable path, because a `prettierConfig` key is the one option name that arrives as a caller value and has to go through `safe` (FR-035); every other option name comes from the catalogue. Both cases are pinned, including that a correctable refusal writes nothing to the log — a typo is the caller's business, and logging every one buries the real failures
- [ ] T110 [P] Add a `patterns` package script aliasing the built binary so the quickstart.md commands resolve before publication
- [ ] T083 [P] Add `conformance:stdio` and `conformance:http` scripts running `@modelcontextprotocol/conformance` against the frozen `2026-07-28` requirement set, and record any unmet requirement with an explicit justification (SC-011). **Blocked, and the assumption behind the task is what blocks it**: the package exists and does carry the frozen requirement set, but `conformance server` reaches an implementation over `--url` alone — there is no stdio target, and stdio is the only transport this server ships. A `conformance:stdio` script cannot be written. Standing up the SDK's HTTP transport for the test would exercise a transport we do not serve, and most of what it then measured would be header and subscription requirements the SDK answers on our behalf. T147 asserts the revision's requirements against stdio directly instead. Revisit if the runner gains a stdio target, or if FR-037's remote transport is built
- [x] T084 [P] Add a stdout-purity test in `test/contract/stdio-purity.test.ts` asserting `stdout` carries only well-formed protocol messages across a full session. Done as an extension to `test/contract/stdio.test.ts` rather than as the named new file, because the property was already asserted in two places and a third copy of the harness would have been the redundancy rather than the coverage: `stdio.test.ts` parses every line of a real framed session and checks that diagnostics are diverted to stderr, and `scripts/smoke-packaged.ts` makes the same assertion against the *built* binary in a spawned process launched from a temporary directory — the only arrangement that can catch a subprocess inheriting the real file descriptor, and it is in the gate. What neither covered was the word "full". Both sessions stopped at discovery, which exercises none of the machinery that writes: generation runs a compiler in a subprocess, executes the emitted tests, and formats every file, and a refusal is the path where our own diagnostics are produced. So the added case runs the sequence — list, describe, generate, refuse, read a resource — and asserts both that every line parses as a frame and that all six requests were answered, since a desynchronised stream loses replies rather than corrupting them visibly
- [x] T085 [P] Add a response-budget test in `test/contract/budget.test.ts` asserting every pattern's `full` response stays within the budget documented in plan.md Performance Goals — 10,000 tokens typical, against the ~25,000-token point at which common agent hosts truncate. That figure lives in plan.md only; the test must read it from one place rather than restating it (SC-008). Measuring first was the right order, because the budget was being missed and not narrowly. Ten of the twenty-six patterns rendered past 10,000 tokens and `chat-model-port` reached about 29,000 — past the truncation point itself, on roughly 88,000 bytes of files. **This is the one silent failure the product has.** Every other way a request goes wrong announces itself; a truncated result cuts mid-file, marks nothing, and the model compiles most of a module believing it has all of it, with no assertion a caller could make to detect it. Fixed by making the *default* rendering self-limiting: when the full text would exceed the budget it summarises instead and says so, naming the levers that return a smaller whole — `includeTests: false`, or `emitScope: core-only` then `binding-only`. Deterministic, since size is a function of the bundle. An explicit `verbosity` is still honoured, `full` included, because the default is ours to choose and belongs on the safe side while a caller who named their preference has answered the question themselves. `structuredContent` keeps every byte either way: summarising is a rendering decision and must not become a data one. The estimator lives in `src/mcp/budget.ts` with the two figures read from one place, and is deliberately pessimistic at three characters per token — under every real tokenizer, because over-counting is the only safe direction for a ceiling and an exact count would pin a permanent behaviour of this repository to one vendor's table while still being wrong for every other host. Swept over all twenty-six rather than sampled: response size is a property of an individual pattern's templates, so a sample says nothing about the one added next month. The text half now passes with no exceptions and should never need one — a pattern failing it has escaped the degradation rather than merely grown. The serialised whole is a separate matter, recorded against T087
- [x] T086 Implement `verbosity` handling — `full`, `code-only`, `summary` — plus `nextSteps` stating what to request when content is omitted (FR-028). The rendering was already there; `nextSteps` was `[]` on every response, so the field an agent is told to read for what to do next was furniture. Three decisions settled it. **Verbosity is not an input to `nextSteps`**, which reads backwards from where the task was filed: the structured result carries every file at every verbosity — the contract is explicit that verbosity governs the content block alone — so a step telling an agent to re-request content it already holds in `structuredContent` would be false, and the surface that omitted something is the one that says so. **A step earns its place only when the bundle is incomplete on its own terms**, which is two cases: a `core-only` bundle is machinery with nothing bound to it and cannot be used as delivered, and a bundle whose `testOutcome` is `skipped` carries the weaker of the two guarantees this project makes. Each names the completing call with its arguments filled in — `emitScope: binding-only`, `coreModule: "./repository-core.js"` in the caller's own extension convention — so acting on it needs no composition. `relatedPatterns` is deliberately absent: `describe_pattern` already carries it, and repeating it on every generation would bury the load-bearing steps under merely interesting ones. **`skipped` is read off the verification record rather than from the absence of a test file**, because a `core-only` bundle returns no suite and was nonetheless executed against one, so counting files would report the opposite of what happened. One rendering change beyond the field: `summary` used to omit the notes, warnings and next steps along with the file contents, so a summarised `core-only` bundle hid the only thing its reader had to do. The contract licenses omitting *contents*, so that is now all it omits — `code-only` remains the rendering that asks for nothing but code. Also dropped the summary line's `diagnosticCount === 0 ? "no" : "some"` ternary, which described a state no returned bundle can be in
- [x] T128 Stop a pattern's own noun from being appended to a name that already ends with it. Asking `branded-type` for a branded `OrderId` — the likeliest single request that pattern receives — was answered with `OrderIdId` in a file called `order-id-id.ts`, and the whole flow this project exists for runs through that moment: an agent picks a pattern, hands over a name, and pastes back what it gets. Fixed as `withNoun` in `options/names.ts` rather than in the pattern that was caught, because seven patterns append a literal noun the same way and are reachable the same way. Two details are load-bearing. The comparison is **by word, not by suffix**: `OrderID` collapses because it is the same name written with the acronym in caps, while `Paid` and `Grid` take the noun as normal, which a plain `endsWith` on the lowercased forms would have swallowed. And both the Pascal and kebab forms come back **together**, because the identifier and the file stem have to agree and deriving the stem separately at each call site is precisely how they had come apart. Collapsing rather than refusing, against this module's stated preference for a refusal over an approximation — that preference is about ambiguity, where a guess bakes in a coin flip, and there is no coin flip in a name that already ends in the noun. Zero golden churn, which is the fix's own proof: the golden entity is `Order`, which ends in none of these nouns, so nothing that was already correct moved
- [ ] T129 Decide whether the collapse in T128 should also apply to the *secondary* nouns each pattern derives, where the overlap is partial rather than total. `withNoun` is applied to every noun in `circuit-breaker`, `repository` and `unit-of-work`, and to the primary artefact noun alone in `factory` and `builder` — so an entity of `OrderCircuitBreaker` gets a correct `OrderCircuitBreaker` breaker and an `OrderCircuitBreakerBreakerPolicy` policy beside it, since `CircuitBreaker` and `BreakerPolicy` share only their middle word. Total overlap is unambiguous and total non-overlap is unambiguous; partial overlap is neither, and guessing which word to elide is the coin flip `names.ts` refuses elsewhere. Either leave it, or refuse an entity name that partially overlaps a noun the pattern will append and say which name to pass instead
- [ ] T087 Implement the oversized-result path: return a coherent subset plus an explicit statement of what was omitted and how to get it, never a silently truncated file (spec edge case). Now has a measured case to answer rather than a hypothetical one. T085 bounded the *text* of every response by summarising the largest, but `structuredContent` still carries every file, so `chat-model-port` serialises to about 29,000 tokens against a ~25,000 truncation point — the only pattern that does, and it is on an explicit exception list in `budget.test.ts` so the overage cannot spread without a decision. Bounding it is what this task is: choosing a subset of files that is coherent on its own, which for a pattern that splits most likely means the core without the binding, and stating what was left out and how to ask for it. The open question is whether the subset should be chosen here at all or whether an oversized bundle should be refused with instructions to request a narrower `emitScope`, since a caller who receives four files out of five has to notice, whereas a refusal cannot be missed
- [x] T088 [P] Author the remaining tier-1 patterns to reach at least 20, weighted toward type-level safety and async resilience (SC-013). The roster below is the agreed one; each entry is a catalog entry, a template, golden snapshots for every documented combination, and a green `pnpm check`. The count SC-013 measures was reached at `unit-of-work` — twenty patterns across six categories — so T088i is what the roster promises rather than what the threshold requires. Done at `context-budget`, which closes the roster: **26 patterns across 7 categories** — `async-resilience` 6, `llm-orchestration` 6, `type-safety` 5, `structural` 3, `behavioral` 2, `creational` 2, `data-access` 2 — against a threshold of 20, and every one of them verified at all three strictness levels and all five other convention axes by the conformance suite
  - [x] T088a `factory` — creational. Key-indexed product map whose lookup narrows the return type
  - [x] T088b `gateway` — structural. Typed boundary to a remote service, transport-agnostic, with one `fetch`-shaped adapter. The pattern turned out to be the six-kind failure taxonomy rather than the encapsulation: a `status` carrying its body, `malformed` and `invalid` kept apart, and `timeout` distinguishable from a caller's `cancelled` — which is only possible because the deadline aborts a controller of the gateway's own. Calls are also *raced* against that deadline, not merely signalled, so a transport that ignores cancellation still cannot outlive its budget
  - [x] T088c `adapter` — structural. A foreign representation reshaped to one the caller owns. Distinct from the `adapter` *file role*, which is a position in a bundle rather than a pattern. Structural typing removes the textbook version of this pattern — an object whose shape fits a port already *is* the port — so what is generated is the part that remains: a `FieldMap` mapped over the target's keys with `-?`, which makes an unmapped field a compile error (verified: a missing *optional* field is an error too, which is the case a plain mapped type would have let through), and per-field failure attribution with a collection index in the name. Two decisions are worth knowing. Mappers report failure by *throwing*, which is unusual here and deliberate: nearly every field mapper cannot fail, and making all of them return a wrapper so the few fallible ones can would be the 90% paying for the 10% — it is also what `JSON.parse` and a schema's `parse` already do. And the combinator is `andThen`, not `then`, because an object with a `then` method is a thenable: an adapter returned from an `async` function would have the promise machinery call it with two callbacks and never settle
  - [x] T088d `builder`, `decorator` — creational and structural. `builder` is done: a phantom type parameter accumulates the keys supplied and `build` is typed so that reaching it early is a compile error *naming the fields still owed*, which is the rendering worth generating; `completeness: result | throw` checks at run time instead, for a builder fed from a form or a config file where the fields it will receive are not known statically. `decorator` remains
  - [x] T088d-2 `decorator` — structural. Not the textbook version, which TypeScript already makes safe: a class declaring `implements` cannot omit a member, so the compiler catches the forgotten forward and generating the delegation would be generating the easy part. What is generated is the case the textbook does not cover — *one* concern across every method at once, where the per-method decorator is the wrong shape and hand-writing it means N copies that drift, plus a method added later that silently receives none of them. The type work is `K extends OrderMethodKeys<Subject>` on the interception, so one function body serves every method while `args` and `proceed` stay that method's own; typed `(...args: unknown[]) => unknown` instead it would compile and make every call site lie. `dispatch` exists because an interface has no members at run time and the two honest answers differ: `manifest` is a mapped type requiring every method, so adding one to the subject is a compile error naming it, and `proxy` needs no list but is an exotic object. Two run-time traps found by building it rather than by reasoning: `Reflect.get(target, property, receiver)` — the spelling MDN's examples use — runs a getter with `this` bound to the proxy, so a class reading a `#private` field throws, and the receiver has to be the target; and spreading the subject to carry its non-methods drops every prototype member, which the emitted suite caught as a getter arriving `undefined`. The manifest rendering forwards through accessors instead, which also keeps a getter running against the object that declared it
  - [x] T088e `semaphore`, `async-queue`, `debounce`, `token-bucket` — async resilience. All four are done. A counting semaphore is four lines of idea and sixty years old; what earns it a place is that on an event loop every way of getting it wrong fails *silently* and arrives as a process that has stopped rather than as an exception anyone can trace. Each defect it defends against was written and watched to fail rather than reasoned about: a permit released after the work instead of in a `finally` leaks on every throw and lowers the limit permanently; a permit released twice raises the count past the capacity, so the limit quietly stops being one; a queue drained from its tail, or a new arrival taking a free permit past a waiter, starves that waiter for as long as work keeps arriving; and a weight above the capacity is a wait no release can satisfy, so it is refused where it is made rather than queued into a deadlock. Two subtleties are worth the option they cost. With `weighted`, the queue head must *block* — serving a lighter waiter behind a heavier one starves the heavy one, so capacity is left idle on purpose — and that rule is only observable when some capacity is free and the head needs more than that, which is why the obvious two-line test for it proves nothing. And a permit is granted synchronously inside `release` while the caller resumes a microtask later, so an abort landing in between finds a caller who already holds one and has already left the queue; `run` checks the signal a second time to close it. Conversely, `#dispatch` needs *no* check for a cancelled waiter: abort listeners run synchronously and dequeue, so such a waiter cannot still be there, and the check that looks prudent is unreachable. The emitted suite uses no timers — each case settles its own promises — and asserts on `snapshot()` rather than on awaited promises, because every bookkeeping defect here strands a promise and a suite built around `await` would report the bug as a timeout with nothing to point at. Verified by mutation: seven defects introduced into the generator, every one caught by the suite it emits, with barging and head-of-line caught only under `weighted` exactly as the code comments claim. Two process notes worth keeping. The conformance suite from T124 paid for itself within the hour — `semaphore` compiled and passed under the default conventions and failed under `testFramework: node-test`, because the `expect` shim needs `{ rejects: true }` to emit the surface these assertions use, and nothing but that suite was looking. And a scripted `str.replace` against this file silently no-oped after Prettier reformatted the line it was matching, leaving `matchersFor` short two matchers; edits to templates want a tool that fails when its target is missing. `async-queue` was written next and the first question was whether it should exist, since it limits the same thing a semaphore does and a bundle is self-contained, so it cannot reuse one. The distinction that settles it is *who holds the work*: a semaphore gates work the caller has already created and started, so its memory is the caller's problem and its order is the order it was created in; a queue is handed the work instead. Every option here follows from that one change and none of them is expressible on a semaphore — a backlog can only be bounded by whoever holds it, a drain signal only given by whoever knows what is outstanding, and work only reordered before it starts. Three findings shaped the code. Equal priorities need an explicit sequence number, not a stable sort: sorting has been required to be stable since ES2019 so priority alone would work today, but the queue would then rest on a property nothing in it states and a later rewrite to a heap would reorder work silently. The space signal has a guard that looks unnecessary and is not — it is reachable in exactly one way, a completion starting a task that synchronously submits another and takes back the slot just freed, and the first three tests written for it all passed against a version with no guard at all. And a full queue *throws* rather than returning a rejected promise, which breaks the rule that a promise-returning function should not throw: refusal is decided about the submission before any asynchronous work exists, and a rejected promise would be lethal to precisely the code most likely to meet it, since discarding it is the point of fire-and-forget and an unhandled rejection ends the process. That last one came out of reviewing the emitted example and noticing its `try`/`catch` could never fire — worth remembering that reading the output found a defect eight combinations of green verification did not. `failures` exists as an option for the same reason: on Node 22 a discarded rejection terminates the process, so a background worker and a collected batch genuinely need different answers, and the wrong one is invisible until production. One claim was withdrawn rather than shipped — announcing idle before starting queued work and after it are provably identical, so the comment explaining the ordering as significant was deleted and replaced with the duller true reason. Verified by mutation: fifteen defects, all caught, but two only after the tests were rewritten. Both had the same fault, and it is the lesson worth carrying: a `.then` handler runs a microtask *after* the signal it observes, so state read inside one may already have moved — the idle case now records ordering instead of counters, and the space case documents why reading them there is sound in that one spot. A third test failed for an unrelated reason worth noting — the priority case submitted six tasks against a backlog limit of four, and the refused one vanished rather than failing, so the shared limit is now a named constant the asserting snapshot derives from. `debounce` was third, and the first pattern here where time is the subject rather than an inconvenience. That decides the shape: the timer is a constructor argument, not the ambient `setTimeout`. Two reasons, both worth more than the ceremony. A test drives the clock instead of waiting on it, so the emitted suite is exact and costs nothing — the alternative is fake timers, whose API differs in every framework this generates for, in a file that also has to run under a hand-written `expect` shim. And the port hands back a *cancel function* rather than a handle, which sidesteps the oldest trap in this area: `setTimeout` returns `number` in a browser and an object in Node, so any type naming the handle is wrong in one of them. Three options, each earning its place from a defect rather than from taste. `edge` is three values and not two booleans so that "neither edge", which never invokes at all, cannot be asked for; its `both` rendering carries the defect the pattern is most worth generating around, since a single call must invoke once and the obvious implementation invokes twice. `maxWait` exists because a plain debounce never serves a caller who never goes quiet, and it brings two traps of its own: reset alongside the quiet timer, neither ever expires and the ceiling silently does nothing; re-armed from the next call rather than from its own expiry, the interval it promises drifts by the gap between calls. `result` is the same fork as `failures` on `async-queue` and for the same reason — a caller who discards the promise still has a failure to route, and on Node an unhandled rejection ends the process — so the `void` rendering requires a handler rather than offering one. The rule for what a caller is told took the most thought and reduces to one sentence: a caller receives the outcome of the invocation *its own call* caused. That is why the three edges assert three different things, why `both` legitimately tells two callers in one burst two different results, and why this does not copy Lodash, which returns the previous invocation's value — stale by construction and impossible to type honestly for an asynchronous function. Fourteen mutations, all caught, four only after rewriting the test. Two of those were the test's own fault and had one cause: checking for a leftover timer *after* advancing the clock, which fires the very timer being looked for. A timer left behind is not merely untidy — in Node it holds the process open — so the suite now asserts on how many are still scheduled, before advancing. The third needed the *times* of invocations rather than their count, since a correct ceiling and a drifting one invoke equally often and differ only in when; the case now uses ticks that deliberately do not divide the ceiling, because with calls landing exactly on it the two implementations agree. The fourth was a claim in a comment that turned out to be false: the fallback for a suppressed caller under a leading edge, marked unreachable, is reached by a call made from inside the wrapped function — and it invoked, defeating the edge from inside the function it wraps. Recording the burst's outcome before calling rather than after fixes it, and the case that proves it is now emitted for every edge, since reentrancy has a different true answer on each. As with `async-queue`, reading the emitted output found what green verification did not: `dedent` had eaten the paragraph breaks and glued a comment onto a closing brace across all twelve combinations. Bodies here are assembled with `sections` for that reason. `token-bucket` closed the group and is the one where the algorithm itself is public knowledge — older than the web, four lines of arithmetic — so what earns generation is entirely the set of choices around it, each of which has a plausible wrong answer. No timer refills the bucket: the level is a function of elapsed time, computed when asked, which means an idle bucket costs nothing, no tick quantises the rate, and there is nothing to shut down. That much is a design choice. The one that had to be *found* is that the level must be derived from a single anchor rather than accumulated: the first prototype added a delta to a stored level on every read, and the case asserting that ten reads across a tenth of a second still yield a whole token came back with `0.9999999999999999`. That is not a rounding curiosity — the tenth grant is refused, so a bucket asked often refills measurably slower than the rate it advertises, and the only symptom is a request denied at an exact boundary. Anchoring fixes it because the error stops compounding with traffic; the saturation cap resets it whenever the bucket fills; and what remains is bounded per grant rather than per query. Elapsed time is clamped at zero for a related reason with a mundane cause: `Date.now` goes backwards whenever the system clock is corrected, which on a fleet is routine, and unclamped that *removes* tokens from a bucket nobody spent from. Order in the waiting rendering turned out to be protected twice over, and discovering that was worth more than either guard. The wakeup is scheduled for what the head needs, and the serving loop stops at a head it cannot afford — and because either alone keeps the queue in order, neither mutation fails a test by itself. Only the pair broken together reorders anything, which is now proved by a combined mutation. The individual mutations were not therefore worthless: scheduling for the cheapest waiter preserves order while waking every millisecond until the head can afford its cost, so the suite counts wakeups and asserts two rather than two hundred — a poll wearing a timer's clothes is invisible to every assertion about *what* was served. And serving one caller per wakeup only differs from serving all it can afford when a wakeup arrives late, which is why the test clock grew an `advanceLate`: timers are a floor and not a schedule, and a busy runtime is the normal case rather than an edge one. Two smaller findings. `retryAfterMs` initially reported the caller's own shortfall and ignored the queue in front of them, which is wrong in the worst direction — it sends a caller back at the one moment it is certain to be refused again, so the limit becomes a source of retries. And a floor of one millisecond on the computed delay, which I removed as provably dead code, turned out to be the only thing standing between a broken invariant and a pinned core: a mutation that asked for the delay about a non-head produced a zero-delay timer that fired, found the same shortfall, and scheduled another. It is back, with the reasoning in a comment rather than the appearance of necessity. The `keyed` option is where the pattern earns the most, because the naive version of it is a map that is never emptied — a leak whose growth is chosen by whoever sends the traffic, which makes it a denial-of-service vector rather than untidiness. The eviction is exact rather than heuristic, and the argument is one sentence: a bucket refilled to capacity is indistinguishable from one that never existed, so dropping it changes no answer. Everything else follows from taking that seriously — the sweep is amortised against a doubling watermark rather than run on every insertion or on a timer, a bucket with a waiter queued on it is never dropped however full it looks, and reading an absent key reports a full bucket instead of creating one, since otherwise a monitoring loop would repopulate everything the sweep just released. Fourteen mutations; the survivors were the more instructive half, since three were unreachable by construction and saying *why* in the comment is better than a test that cannot fail. Two process notes. The first stranded-key test advanced the clock far enough to serve the very waiter it meant to keep waiting, so it asserted zero and passed for the wrong reason until verification caught it — a hazard specific to suites that own their clock. And `try-render.ts` had been carrying its own copy of the module registry, which had silently fallen five patterns behind; it now imports the one in `generate/index.ts`, and `indent`, which `debounce` had reimplemented locally, comes from `render/helpers.ts` in both.
  - [x] T088f `branded-type`, `discriminated-union`, `typestate`, `parse-dont-validate` — type-level safety. All four are done. This group is a different kind of work from everything before it, because the artefact is the type, so the first question was not what to generate but how a generated bundle can assert that something *fails* to compile — no suite that runs can tell a brand that still rejects a raw string from one that has stopped, since both behave identically once anything is running. Two vehicles, settled once for the group. `@ts-expect-error` states that a line must not compile and belongs on code a caller would plausibly write; it is an assertion rather than an apology, because if the guarantee lapses the directive goes unused and the file stops compiling on that instead. Named aliases over `Equal`, `Extends` and `NotAssignable` state what a type *is*, which no directive can. Three facts about the existing engine made this cheap, and all three had to be checked rather than assumed: every file in a bundle is typechecked, including tests; only files ending `.test.ts` are executed, so a `*.test-d.ts` is read by the compiler and run by nothing — which is also the established convention for the purpose; and the kit is inlined rather than emitted as a shared file because, unlike the run-time `expect` shim, exactly one file per bundle needs it. The `Equal` alias is shared in the generator for the reason `expect-file.ts` gives: the two-function form is subtle enough that four hand-written copies would not all have been right, and the obvious alternative — mutual `extends` — answers a different question, reporting `true` for `any` against anything and distributing over unions. One rule constrains every assertion and is easy to miss: never assert about nullability. Bundles are verified at every strictness, and with `strictNullChecks` off `undefined` is assignable to everything, so `Equal<string | undefined, string>` is false under `strict` and true under `loose` — an assertion whose meaning depends on the reader's compiler options is not one. Every claim these patterns make is therefore about nominal identity or a missing member, and the prototype was run at both extremes before any of it was generated. For `branded-type` itself the carrier is a `unique symbol` rather than the string tag the technique is usually shown with, and that was verified both ways rather than argued: two tags declared independently with the same text are structurally the same type, so two modules that both reach for `"Id"` get identifiers that interchange with no diagnostic anywhere, while distinct symbols cannot collide however their labels read. The cost — the type must be imported rather than restated — is the intended constraint. `base: number` exists for a defect the string case does not have: arithmetic silently discards the mark, so a sum of two valid quantities is a plain `number` and has to go back through the constructor. That claim is stated about the sum itself rather than about `number` in general, because the first version restated an earlier assertion on the same page and proved nothing new. `construction: cast` emits no rule at all, which is the honest rendering for a key read back from the store that wrote it — inventing a validation rule at that boundary is theatre that fails valid input — and in every rendering the unchecked constructor is spelled `unsafe…` because it is the one hole in the guarantee and the name is the only thing that makes it visible in review. The run-time suite asserts the complement of the type-level one, and it is worth more than it sounds: that the mark is *erased*, so the value still compares with `===`, still works as a `Map` key, and still survives `JSON.stringify` — a brand that broke any of those would be a run-time cost disguised as a type. Four mutations against the generator, all caught, and the counts were the interesting part: dropping the brand fails all twelve combinations, aliasing the two sibling brands together fails all twelve, and a guard returning `boolean` instead of a predicate fails all eight validating renderings — including the ones with no tests, because the constructor narrows rather than casting, so the core itself stops compiling. That last one confirms the "no cast in sight" comment is load-bearing rather than decorative: written with `as`, the constructor would compile whether or not the check above it was correct. Three process notes. The verification sandbox types `toBe(expected: T)` against whatever it was given, so `expect(branded).toBe("ORD-1")` is a compile error — a genuine catch, and the fix reads better than the original, since binding to a base-annotated local is itself half the assertion that the brand widens without a cast. The comment reflow from T114 will re-wrap a long `@ts-expect-error` comment and a directive only governs the line it begins on, so the directive now sits alone with its reason above it. And the catalog's own validation caught `relatedPatterns` pointing at three patterns in this group that do not exist yet; the back-links go in as each lands. `discriminated-union` came second, and two of its three interesting findings contradicted the obvious answer. The record dispatch needs no cast: indexing the handler map with the value's own discriminant collapses the parameter to an intersection of every member — `never`, since the discriminants conflict — which is why the technique is nearly always shown with an internal `as never`, but making the function generic in the discriminant preserves the key-to-member correspondence and the plain call typechecks, with a missing key, a surplus key and a handler reading another member's field all still rejected. The case for named predicates is not the one usually given either: `filter((event) => event.kind === "shipped")` does narrow now, through inferred type predicates, and what destroys it is the explicit `: boolean` return annotation that explicit-return-type lint rules require — so the conflict is between two things a codebase wants and the silent loser is narrowing. Stating `event is OrderShipped` satisfies both. The third finding is about method rather than about types, and it invalidated a set of results before it fixed them: the golden suite is the wrong instrument for mutation testing, because it fails on *any* change to the output and so cannot distinguish an assertion firing from stored bytes moving. Run that way, seven mutations all looked caught and two of the counts were wrong. `scripts/verify-combinations.ts` exists for this — it reports the verification outcome per combination and nothing else — and re-measured, the same mutations showed two real gaps: dropping a constructor's declared return type, and restating the discriminant list instead of deriving it, were both caught only where tests were emitted. Closing them improved the artefact rather than the suite. The example had never actually constructed an event, so it now builds a history through the constructors — an array whose annotation is load-bearing, since without the declared return types `kind` infers as `string` and the most ordinary line in the file stops compiling — and tallies events into a `Record<OrderEventKind, number>`, which is a second place completeness is enforced structurally and the only place it is enforced in the `switch` rendering. Both mutations now fail all eight combinations. Mutation testing also caught an overclaim in my own comment, which is the more useful kind of catch: `match`'s doc asserted that the `as never` version weakens checking for callers, and mutating to that version verified cleanly, because the handler map's type constrains every handler at the call site either way. The comment now says what is true — the difference is internal, and a caller cannot tell them apart. One recurring lesson, second instance: the verification sandbox's assertion shim is narrower than the real framework's, and an emitted suite has to fit it. `toBe` is typed against whatever it was given, which caught a raw comparison against a branded value; `toThrow` accepts only a string or a RegExp, not an error class, so the unknown-kind case matches the message and establishes the class through `instanceof` in the case after it. `typestate` came third, and the compiler contradicted the obvious design twice before any of it was written. A state parameter that no member mentions is inert: `Thing<"idle">` and `Thing<"open">` are then structurally identical, assign to each other in both directions, and every `this` constraint on every operation silently stops applying — the pattern compiles, reads exactly as though it works, and forbids nothing at all. The state is therefore anchored by a real, readable field, which costs one property and is worth having anyway for logging. The second is sharper and decided what the module exports: on a value typed `Order<OrderState>`, testing the state field narrows the *field* and leaves the type argument alone, so the state-specific operations stay uncallable and a caller holding one has no route back to them and nothing indicating why; on a union of instantiations the same test narrows properly. The two spellings look interchangeable and only one can be narrowed, so the emitted `AnyOrder` is the union — and mutating it to the form anyone would write first fails four of eight combinations, which is the claim pinned rather than merely commented. Third, a `this`-constrained operation cannot be detached from its receiver, though it passes as a closure, which is worth knowing before designing an API around one. The limit that cannot be closed is linearity: a transition supersedes its input and TypeScript has no way to say so, so `order.submit()` twice type-checks and returns two values the types are perfectly happy with. That is what `staleGuard` is, and it defaults on because the constraint has to live somewhere — a `WeakSet` rather than a flag, so the types stay `readonly` and nothing about the guard leaks into what a caller serialises, and so transitioned values are not retained. The `representation` option turns on something more interesting than syntax: a private constructor makes the parameterised states unreachable except through the transitions, whereas plain interfaces are satisfied by any object literal, so the distinct form guards the operations and not the states. That asymmetry is why `parameterised` is the default and why the emitted example asserts the fabrication refusal only there. Eleven mutations, all caught; the two that only fail two of eight combinations are the run-time behaviours — the guard actually refusing, and items surviving a transition — which no compile-time assertion can express, so tests are the only place they can be pinned. Three process notes. Writing `documented(...).concat("", documented(...))` where `sections(...)` was meant glued a doc comment onto the previous closing brace, and verification passed because Prettier reformatted it into something legal — the third time now that reading the emitted output has found what eight green verifications did not. The emitted suite reached for `.not.toThrow()`, which is not in the `expect` shim's deliberately closed matcher set, and the conformance suite caught it under `node-test`; the repair was better test code regardless, since asserting the state reached says what happened where asserting the absence of a throw says only what did not. And renaming two emitted type aliases after the determinism record had been written left that record stale and failed the gate — cheap to fix, but a reminder that a rename touches both snapshot families and the blast-radius record is the one easy to forget. T125 was opened rather than fixed here: sweeping this pattern across the framework axis by hand reported `jest` as *not accepted*, and all sixteen modules carry a dead `@jest/globals` arm for a value the schema does not permit. `parse-dont-validate` closed the group, and unusually for this catalogue the general argument for it was already well known while the evidence was not, so the work went into making the claim checkable rather than making it. It reduces to one line: `if (items.length > 0)` does not refine `readonly string[]` into `readonly [string, ...string[]]`, verified at all three strictness levels, so a validator genuinely cannot hand its knowledge to the next function and rebuilding through a parser is the only route. What that buys was checked too, and is the reason the refinement is a tuple rather than a nominal wrapper: the first element is `string` and not `string | undefined` even under `noUncheckedIndexedAccess`, so a function over parsed input is total and the empty branch is *gone* rather than merely unlikely. The no-nullability rule cost real assertions here for the first time — that `.at(0)` stays partial and that any element past the first does are both true, tempting, and inverted by `strictNullChecks: false`, so both were dropped and the omission is recorded in the emitted file so nobody adds them back. Two limits are asserted instead of mentioned, because assuming otherwise is the likely mistake: `map` and `filter` both widen the refinement away, so a derived collection is parsed again. The error channel applies the pattern to itself — an accumulated failure carries a `NonEmptyArray` of problems, so `problems[0]` is total and no reporter has an empty case — and that spread composes without an unreachable branch, because a new tuple built from a known-failed field's problems is provably non-empty. `combinators` earns its place on something firmer than taste: the kit derives the parsed type from the parser, which removes the one defect the straight-line form cannot rule out, a hand-written type and its parser drifting until the type promises a field nothing checks. Its one cast is the honest kind — a heterogeneous record filled key by key is beyond what the compiler tracks — and tightening it was worth the trouble: iterating keys and indexing yields a possibly-undefined parser whose obvious handling, skipping the key, would leave a field unparsed and make the cast a lie, where `Object.entries` cannot produce one. The constraint on that combinator also has exactly one correct spelling, found by getting it wrong in four of eight combinations: parsers are covariant in what they produce, so the bound is `Parser<unknown>` and `Parser<never>` accepts nothing at all. Fifteen mutations, none missed, and the counts located a real defect that eight green verifications had hidden. Widening the refinement — the single most important thing this pattern claims — was caught in only six of eight combinations, and the two survivors shared a cause: in the `errors: first` rendering the example named `NonEmptyArray` without importing it, and because every use sits under a `@ts-expect-error`, the directive absorbed "cannot find name" and three refusals passed for a reason unrelated to what they assert. That is precisely the hazard the emitted type-test file warns about in favour of `NotAssignable`, met in my own generator; the import is now unconditional with the reason recorded beside it, and the mutation fails all eight. The lesson generalises past this pattern: a directive-based assertion in a file whose imports vary by option is only as good as the weakest rendering, and mutation counts are what surface it, since the count and not the pass is the signal.
  - [x] T088g `specification`, `typed-emitter` — behavioural. Both are done. `specification` opens the `behavioral` shard. The classical treatments stop at composability, so the work went into the part TypeScript can add: the rule carries the refinement it establishes, and `filter` over it yields a list whose element type records that the rule held — which is what makes a function requiring a paid order accept the result and refuse an unfiltered list. Four facts were settled with the compiler before any of it was written. Composition keeps the refinement, `and` intersecting and `or` unioning, through chains three deep and through a variadic form whose non-empty tuple constraint also makes the meaningless empty call an error. Negation cannot keep it, and the type that appears to is worse than none: `Exclude<Order, PaidOrder>` is not the complement but `Order` itself, since `Exclude` distributes over unions and `Order` is not one — and because TypeScript verifies no predicate's *logic*, the version declared that way compiles and hands back the unrefined type under a name claiming otherwise. So `not` gives the refinement up in its signature, which is asserted rather than commented. A leaf declared as data still earns a refinement from its own value, so choosing translation costs nothing in precision while an unknown field, an impossible value and a magnitude comparison against text are each refused. And the refinement survives only when a rule is *named* before being combined: inferred inside a nested call the literal widens to the whole property type with `strictNullChecks` off. The idiomatic form — rules have names, which is the point of the pattern — is also the only one whose types hold everywhere. The `translation` axis is what separates this from a named predicate, and its price is worth stating: the leaves must be declarative, because a rule expressed as a closure can be run but not read. What it buys back is that one declaration produces both the predicate and the query, so the in-memory answer and the pushed-down answer have no second expression to drift from. No escape hatch for arbitrary predicates is offered when translation is on, because a partial `toQuery` would make every caller handle a case the constraint can simply prevent. The SQL renderer parameterises values and splices field names, which would be the obvious injection hole and is closed by the type rather than by escaping: `field` is `keyof Order`, so the only strings that can reach it are property names of a type declared in the same file. Twenty-one mutations, none missed, but the counts earned their keep twice. An inclusive comparison mutated to an exclusive one survived, because the fixture's totals were 10 and 500 against a threshold of 100 and nothing sat *on* the boundary — the one input where `>` and `>=` differ. Fixing it also reached `whereAtMost`, which the suite had never called at all. Then the gate caught what the per-pattern script could not, and it is the most useful finding here: all eight combinations failed at `strictness=loose`, including the ones with no tests. `strictFunctionTypes` is part of `strict`, so without it function parameters are compared bivariantly and a callback wanting the *narrower* type is accepted where the wider one is expected — which means `orders.map(receipt)`, the obvious way to assert that an unfiltered list is refused, compiles under a loose config and leaves the directive unused. The assertion is now a direct call, where argument assignability is invariant everywhere, and the negation claim is an array assignment for the same reason; both hold at all three strictness levels, and weakening `receipt` makes both go unused, so they are live rather than decorative. The process lesson is concrete: `verify-combinations.ts` checks one strictness unless told otherwise, so a pattern whose claims are compile-time needs the loose and strictest sweeps *before* the gate, not from it. Two smaller notes. The `a ${lower}` construction produced "a order" again, in the one doc comment that interpolated the entity after an article; the phrase was rewritten to not need one. And no assertion here concerns nullability, for the reason established in T088f. `typed-emitter` closed the group, and its most useful result is a negative one about a cast almost every published implementation of this carries. Storing handlers in a `Map` keyed over the union of event names forces a lookup at `K extends keyof E` to collapse to the intersection of every payload type, so dispatch needs `as never` to proceed; storing them in a mapped type keyed by the event name instead makes the indexed lookup yield exactly that event's handler type, and nothing emitted here contains a cast. That took one correction to arrive at, and it is a real constraint rather than a preference: the registry is only cast-free while the class is *generic* in its event map, because writing to a concrete mapped type at a generic key is refused, so the base class is `TypedEmitter<E>` and the concrete emitter is a subclass that fixes `E`. Both halves were verified at every strictness — an unknown name, a wrong payload type, too few arguments and a handler whose parameters disagree are each refused, while a handler ignoring some or all of the payload is *accepted*, which is ordinary function assignability and worth knowing before anyone files it as a bug. Payloads are variadic tuples with named elements, so parameter names show up at the call site and a zero-argument event emits with no arguments at all. Three run-time decisions were settled by prototype, each a place emitters are commonly wrong and none of them visible from the types: the handler list is snapshotted before dispatch, so a handler registered by a handler does not run in the dispatch that registered it; a handler removed during dispatch does not run either, because unsubscribing should mean unsubscribed even from an emit already in flight; and `once` is a flag on the registration rather than a wrapper, because a wrapper has a different identity from the function the caller passed, which would make `off` with that function silently do nothing. The `errors: isolate` default requires its sink rather than offering one, for the reason `async-queue` and `debounce` established about discarded rejections, and for a second reason specific to this pattern — the premise of an emitter is that the emitting code does not know who is listening, so letting an unknown subscriber's failure surface inside it couples them straight back together. Seventeen mutations, all caught, one only after understanding why: dropping the `await` in the dispatch loop is invisible under `propagate`, since a rejection still reaches the caller, and only the `isolate` suite distinguishes them. Two type-level frictions are worth recording because both cost a full verification cycle. A handler returning `void | Promise<void>` does *not* accept a concise arrow whose body evaluates to a value — the special assignability rule for a bare `void` return does not survive being put in a union — so `() => seen.push("x")` is an error and every handler in the emitted suite has a block body, with the reason stated in the file rather than left for the next person to rediscover. And the `expect` shim's `toThrow` takes a string or a RegExp, which the `node-test` rendering narrows further to a RegExp only; rather than branch on the framework, error assertions now go through a helper that captures the failure and compares its message, which reads better and is identical across `sync` and `async` where `toThrow` and `rejects.toThrow` would not have been.
  - [x] T088h `unit-of-work` — data access, split-capable alongside `repository`. Done, and it splits along the same seam as `repository` for the same reason (FR-017): the change tracking, the store seam and the in-memory store mention no domain type, while one entity's record constraint, collection name and typed accessor are a fraction of that beside it. Three type-level results were settled with the compiler before any of it was written. Constraining the *entity* rather than the field — `T extends Storable<F>`, with the key field's name as a type parameter — is what keeps the file free of casts: the obvious spelling, a `keyof T` filtered to its string-valued members, names the right fields and leaves `T[F]` unreducible, so reading a key needs an assertion where the constraint form makes `record[spec.keyField]` an ordinary indexed access, and `Omit<T, F>` a patch type that refuses to change a record's identity. A spec carries a phantom property in its entity, because two collections keyed on a field of the same name are otherwise the same type, and a function annotated for one would accept the other's spec and hand back records of the wrong type. And the region trick that would keep two open units of work's records apart does not work here: the scope parameter is inferred at the call site, so two nested scopes receive the same one and their records interchange with no diagnostic anywhere — TypeScript cannot spell the rank-2 type it would need — which is why refusing a committed unit of work is a run-time check, exactly as `typestate` says about any lifecycle whose value escapes into a closure. The store seam takes a *batch* rather than handing out a transaction, and the trade is stated in the emitted file rather than hidden: `apply(operations)` is implementable on a datastore with no interactive transaction and does not hold a connection open while domain code runs, at the cost that reads happen before the commit and are therefore outside it — which is the entire reason `concurrency: "version"` exists. Two findings about *what to emit* came from the strictness sweep and from mutation, and both changed the artefact. `Patch` is a mapped type with `| undefined` on every field rather than `Partial<Omit<…>>`, because `Partial` alone refuses an explicit `undefined` under `exactOptionalPropertyTypes` — so a caller spreading a form or a partial DTO would compile under one configuration and not another, while the method is documented to treat that value as no change. And the write-back after a successful commit turned out to contain dead code: a mutation that stopped `loadedVersion` being refreshed survived, and the reason is that a committed unit of work refuses every method that could read it again. Deleting it was the easy half; the useful half was that `plan` is the one method a closed unit of work still answers, so settling has to make the plan *empty* — which it did not, because a record that had been deleted stayed in the sequence and kept producing a delete operation forever. Both are now pinned by a test rather than by a comment. Two more assertions were weaker than they looked. The case proving that a failed commit leaves the unit of work open asserted only on `plan`, which does not check whether it is closed, so closing early survived; it now shows that a further load succeeds. And the field-comparison helper skipped the key field as well as the version, an exception no mutation could kill because the key check ran first — moving that check out of `commit` and into `planned` made `plan` refuse a moved key too, let the exception shrink to the version alone, and made *that* killable by a case which assigns to `draft.version` and asserts the write never happens. Twenty-four mutations, none surviving, and the ones worth naming are the ones about what is not written: an unchanged record producing a write, a record added and then removed leaving an insert behind, a patch replacing rather than merging, an `undefined` counting as a change, a second load handing back a second handle, and the in-memory store writing before it has checked the whole batch — that last one being the contract the pattern rests on, since a half-applied commit is the failure it exists to prevent. The two tracking renderings are a real fork rather than a syntax preference, and the shallow comparison is where `snapshot` earns its caveats: replacing a nested object is a change and reaching into one is not, because the draft and the copy taken at load hold the same reference. Demonstrating that at all required the fixture's nested field to be mutable, since `Draft<T>` removes `readonly` at the top level only and a `readonly` inner object cannot be reached into in the first place — the test that would have proved the rule did not compile until the entity was written to allow it. `readonly` on the key is no defence either, for the reason T088f established about assignability, so a draft whose key moved is refused when the commit runs rather than where the line was written, and the emitted comment says so. Four process notes. The accessor and the obvious local variable in the example were both `orders`, which is a hazard specific to generated code: every other name in the file is derived from the entity, so a local has to be chosen from outside that space. A `@ts-expect-error` governs the line after it, so a *property* that must be refused inside an object literal needs the directive on that property — putting the call on one line is not a fix, because Prettier decides where the lines fall. `rejects.toThrow` is deliberately absent from the `expect` shim, so the abandoned-transaction case asserts on the rejection's class instead, which is better anyway; three sync refusals moved the same way, through a helper that returns whatever a call refused with, so the assertions are about which refusal it was and not about its wording. And an object literal getter needs a comma after it — the first render failed to parse, which is a reminder that a template is not typechecked by the compiler that checks its output. One fix outside the pattern: the gate reported the conformance suite as failed after all 1045 tests in the run had passed, because the per-file teardown that releases the warm compiler exceeded Vitest's ten-second hook timeout on a machine the conformance file had been saturating for nine minutes. It is now a minute, which is a ceiling on a wait rather than a budget.
  - [x] T088i The `llm-orchestration` group, on the seam settled in research §13. `chat-model-port` is done — split-capable, the port as the core and each vendor adapter as a binding — and opens the `llm-orchestration` shard; `stream-accumulator`, `structured-output`, `tool-loop`, `model-middleware` and `context-budget` are done beside it. Three results were settled with the compiler before the port was written. A `const` type parameter on `generate` keeps an inline array of tool literals from widening, which is what makes `toolChoice` refuse a name no tool declared without the caller writing `as const` — and the default for that parameter has to be the empty tuple rather than `readonly ToolDefinition[]`, since the wider default collapses the name union to `string` and accepts anything. That guarantee does not reach a tools array hoisted into a variable, and cannot: it widens where it is *declared*, so a shared list wants `as const satisfies readonly ToolDefinition[]` on its own declaration, which is what the example does and why the suite carries both cases. And the system prompt is a top-level field rather than a fourth message role, because only one of the two wire formats can place one mid-conversation — as a role it would be a request that typechecks and cannot be sent. Both adapters ship in full rather than one plus a sketch, because absorbing the differences is the entire claim: a system prompt as a first message against a top-level field, one message per tool result against `tool_result` blocks inside a user turn, tool arguments as a JSON string against a value, `required` spelled `any`, a required output limit, and consecutive same-role turns merged. Each of those is a line in one file here instead of a branch in the code that calls the model. Two findings from verifying it are worth keeping. **The suite's real subject is the framing, not the port.** Encoding and decoding are pure functions over recorded bodies, so they are cheap to pin; the stream reader is where a hand-written version fails, and every framing case in the emitted suite is a shape a real stream produces — a data field split across chunks, an event whose data spans several lines, a keep-alive comment, a stream that ends without its final blank line, and a multi-byte character split across two reads, which is the bug that only appears in the languages nobody tested in. **A mutation pass over 50 defects found three gaps and one piece of dead code**, all now closed: the `core-only` scope never constructed either error class, so `MalformedResponseError`'s never-retryable contract went unasserted in the one scope where no decoder asserts it for free; the `providerOptions` merge order was pinned only against a field written *before* both, so moving the merge above the streaming flags survived, and it is now pinned on the last field either encoder writes; and the explicit branch skipping an SSE comment line was behaviourally equivalent to letting it fall through the field dispatch — which is what the specification actually asks for — so it is gone and the reason is in a comment where the branch was. The harness itself had to learn one thing: several anchors appear in both adapters, and a string replace that hits only the first makes a live mutant look dead. Two shared files moved for this pattern. `src/engine/verify/platform-types.ts` gained `Response.body`, `ReadableStream` and `TextDecoder`, declared as narrowly as the rest (T116) so a streamed body can be read without the pattern inventing its own decoder interface; and `provider` declares `files` in `affects`, because each adapter is named for the format it speaks so two can sit beside one port — which the diff-stability suite caught as an undeclared rename before a reviewer could. **`stream-accumulator` followed, and its interesting result is that it needs no dependency on the port.** TypeScript is structural, so the part shapes are declared in the emitted file and a stream from the port's adapter fits anyway; assignability was checked with the compiler in both directions before the templates existed, and what the fold returns is mutually assignable with the port's `ChatResponse` under `exactOptionalPropertyTypes` — which is what lets a tool loop have one code path for a streamed turn and a waited-for one. The looseness runs one way on purpose: `warnings` on the finish part is optional here and required there, so a hand-written producer with no notion of warnings fits while the port's own parts stay valid. Two smaller results came out of the same probes. `for await` accepts a sync iterable as readily as an async one, so the signature takes both and a recorded array of parts replays with no helper — the form a log hands back, and the form the emitted suite uses throughout. And the snapshot hands over the whole text rather than the latest delta, because a UI that appends has to have appended every one and a dropped delta leaves a hole nothing later corrects. **The mutation pass, 33 defects over three combinations, found two gaps** — both about what happens *after* something ends, which is the class of bug a fold is prone to. Deleting a finished call from the open map looked like housekeeping and is not: without it a fragment arriving after `tool-call-end` is silently appended to the closed call, and the refusal then surfaces several parts later as input that will not parse. The suite now refuses it where it happens and pins the offending part, since asserting the error class alone passed under both behaviours. And the snapshot's text was only ever exercised on a single run of text, so accumulating it was indistinguishable from replacing it; a case with a tool call between two runs kills that mutant. Every one of the 33 is now caught by the emitted suite rather than by the compiler — one was initially caught only by `noUnusedLocals`, which is a mutant the tests did not actually notice, so it was reworked into an inverted condition that they do. **`structured-output` came third, and its whole premise is one interface declared rather than imported.** Standard Schema v1 is a single `~standard` property carrying a version, a vendor name and a `validate` function, and Zod 3.24+, Valibot 1.0+ and ArkType 2.0+ all implement it — so fifteen hand-written lines let a caller pass `z.object({ … })` in and get its inferred output type back out, with no dependency in either direction. That is exactly the kind of claim that is either verified or wrong, so it was checked against `zod@4.4.3` in this repository under `exactOptionalPropertyTypes`: assignability *and* inference, plus the shape of the `issues` a real library returns, since the path segments this renders come straight out of one. The model seam was verified the same way and is narrower than the port: `StructuredModel` is one method over the fields this pattern sets, and the port's `ChatModel` satisfies it including the awkward part, where the port spells a named tool choice against the literal union of the tool names it was given and this seam spells it `string`. Written as a function property rather than a method that assignability would run the other way under `strictFunctionTypes`, which is why it is a method. Three findings shaped the emitted code. **The JSON Schema is a second argument and that is not a wart:** Standard Schema deliberately carries no JSON Schema export, so there is nothing to derive the wire shape from, and deriving it would mean importing the converter the interface exists to avoid — so the caller passes both and the docs say plainly that they have to agree. **The correction differs by strategy, and getting it wrong is a provider error rather than a bad answer:** under `tool-call` the failed attempt is an assistant message containing a tool call, and both mainstream wire formats reject a following request in which that call has no matching tool result — so the complaint travels *as* the tool result, `failed: true`, carrying the call's id, where the text strategies send a user message after an assistant echo. A hand-rolled loop that sends a user message in both cases works until the first time it retries a tool call and then fails with a 400 that says nothing about schemas. And `response-format` writes its instruction into `providerOptions` because the port's request has no field for it, which is the honest shape rather than a shortcut: JSON mode is not part of the subset every provider agrees on, Anthropic has no equivalent at all, and that is why `tool-call` is the default here rather than the one everybody reaches for first. **Both of the loose-strictness narrowing traps this catalogue has met turned up in one file.** The boolean-literal one is T122's and was fixed the same way. The second is new and worth recording, because it is not ours to change: Standard Schema's discriminant is the *absence* of `issues`, spelled `issues?: undefined`, which narrows under `strict` and stops narrowing when the compiler is not keeping `undefined` — so `validated.issues === undefined ? validated.value : …` reports that `value` does not exist on the union in a project with `strictNullChecks` off. A one-line type predicate states the conclusion instead and holds at every strictness, and it is declared once beside the type rather than inlined at each of the two reads. The probe that found this also found the harness lying: `tsc --ignoreConfig` without an explicit `--strict false` does not reproduce a loose project, so four narrowing forms all appeared to compile and the sweep in `verify-combinations.ts` was the only thing telling the truth. **A mutation pass over 41 defects across the four renderings found three gaps**, and each was a test asserting less than it read as asserting. An answer with nothing in it was pinned only by its error *class*, which is the same class the no-JSON-found path throws, so disabling the empty check entirely survived — it now asserts the message, because "no JSON was found" describes the wrong problem to whoever has to go and look. The fence is searched before the whole answer precisely so that a brace in the prose around it cannot win, and no case had prose with a brace in it, so ignoring the fence survived; one now does. And `failed: true` appears in the template *and* in the test that asserts it, so a naive replace-all changed both and the mutant looked dead — the anchor now carries the line above it. Two smaller notes. Reading the emitted output found what green verification did not, for the fourth time in this catalogue: the test suite's cases ran together with no blank line between them, because `describeBlock` joined them with newlines and Prettier preserves blank lines rather than inserting them. Fixed here and in the two siblings, which had the same defect. And the assertions on rendered issue paths were substring checks that never looked at the front of the line, so a path emitted with a stray leading dot passed; they now include the bullet, which is what makes the `pathOf` mutants killable. **`tool-loop` came fourth, and the question it exists to answer is how a box of unrelated tools hands each executor its own input type without a cast.** Three spellings were tried against the compiler before a template was written, and two of them cannot work: a `ToolSpec<Input, Output>` stored in a shared object either widens the executor's parameter to `unknown`, which puts the cast back in every tool, or is stored as `ToolSpec<never, unknown>`, which the parameter's contravariance refuses. What does work is a factory: `tool<Input, Output>` closes over `Input` so `parse` and `execute` agree *inside* it, and returns a `Tool<Output>` that no longer mentions the input at all — so a heterogeneous `Toolbox` is an ordinary object and dispatch by the name the model sent needs no assertion anywhere. Two consequences are deliberate. `parse` is required rather than optional, because the input is whatever the model decided to send and `inputSchema` is a hint the provider may not have enforced, so the executor is only reachable through a parse. And a tool carries no `name`: the name is its key in the box, which is the one arrangement where a tool cannot disagree with the name it is registered under. The model seam is a method for the same reason `structured-output`'s is, and `ToolFailedError` carries what a tool threw through `super(message, { cause })` rather than a field of its own — a declared `readonly cause` collides with `lib.es2022.error` under `strictest`, which wants `override` on it, and the standard shape is shorter than the thing that provoked the diagnostic. **The defect worth recording was found by a test about handing the transcript back, and it was aliasing.** The loop appends to one array and passed that array to the model, so every request the model had already been given kept changing underneath it: a recorded request showed turns that had not happened when it was sent, which makes a log useless and a replay impossible. Each request now takes a copy, and a case asserts that a request the model was given does not change afterwards — an assertion that reads like paranoia and was not. **A mutation pass of 68 defects over five renderings taught more about the harness than about the pattern.** Twelve mutations were reported as survivors and none of them were: `tsx` caches its transpile per file, this harness rewrites one file 68 times in a row, and a stale hit runs the *unmutated* generator, so the suite passes and the mutant looks alive. With the cache off, 64 of the 68 were caught immediately. The lesson generalises to any mutation harness that edits one file in a loop, and the reassuring half is that the failure is one-directional — a stale hit can only invent a survivor, never hide one — so the three passes before this one, which reported no survivors, were never at risk. The cache is now disabled inside the harness rather than remembered. What remained after that was one real defect and three tests asserting less than they read as asserting. `stepOf` wrote `warnings: []` unconditionally, so a step always reported that the provider had said nothing about the turn, which is worse than having no field at all. `textOf` joins the runs of text in a turn with nothing between them, because they are pieces of one sentence and anything between them lands mid-word — and no case had a turn with two runs, so joining with a space survived; the case that now covers it splits a word in half so the difference is unmissable. `hasToolCall` asks about the turn that just ran, and every case had exactly one tool-calling turn, which is simultaneously the first and the last, so reading `steps.at(0)` survived; a run whose first turn calls a different tool kills it. And `repeatedCall` keys on the tool *and* its serialised input, which no case distinguished from keying on the input alone, since none called two different tools with the same argument — a model asking two tools about the same order is not repeating itself, and there is now a case saying so **`model-middleware` came fifth, and the only hard question in it is how to wrap a generic model without quietly making it a lesser one.** The request and the answer are treated in opposite ways and that asymmetry is the design. A layer *writes* requests, so the message union is declared in full: a shorter declaration — a message as `{ role: string }` — accepts everything the port sends and also lets a layer invent a user turn with no content, which typechecks and fails on the wire. A layer only ever passes an answer *along*, so the answer is a type parameter bounded by the one field anything here reads, which is what stops a wrapped model returning less than the model did. **The failure that took the longest to find is that a single non-generic layer collapses the answer for the whole chain, and nothing reports it.** `wrapModel` infers its answer from two places, the model and the list, and a `Middleware<MinimalAnswer>` in the list wins — after which every caller of the wrapped model has an answer with nothing on it but `usage`. So every layer factory is generic in the answer and `wrapModel` takes its list as `NoInfer<…>`. Both halves were checked against the compiler on the emitted file rather than reasoned about: with `NoInfer`, a layer written over the bound is refused at the call site with a message naming the field it would have lost; with it removed, the same call is *accepted* and the only error is three lines later where a caller reads `text`. That is the whole argument for the utility in one diff. **The port's tool-name checking survives wrapping, and the obvious implementation loses it silently.** `generate` is generic in a `const` tuple of tool definitions, so `toolChoice: { mode: "tool", toolName: … }` is checked against the names the request declared; the wrapper keeps that narrow signature outside and runs one widened request inside, which works because the request type is covariant in its tools. Written the natural way — a single non-generic request parameter — the wrapper still typechecks and the guarantee is gone with no symptom until a provider returns a 400, so the emitted `*.test-d.ts` states it as a call a caller would plausibly make. One smaller decision: there is no per-call context object, because a wrapping hook has both halves of a call in one closure, and the correlation id a middleware stack usually needs a context for is a local variable here. **Three of the four layers turn on what happens after something ends.** A stream is not an answer, so the cache stores two kinds of entry — an `AsyncIterable` handed back twice is empty the second time, and what a hit replays is a fresh iteration over kept parts. It stores them after the loop rather than as they arrive, so a stream that failed half-way is not filed as a whole answer and then replayed without its failure forever. Telemetry reports after the loop for the mirror-image reason: in a `finally` an abandoned stream is reported as one that finished, which is a latency figure nobody can account for. And the fallback layer's rule for a stream is stricter than for a call, necessarily — once a part has been handed over the caller has rendered half an answer, and a second model would begin a different one, so a stream that failed after its first part fails. The one ordering caveat is pinned in both directions: `withDefaults` has to sit *above* `withCache`, because from inside the cache a request whose temperature has not been filled in yet is not repeatable, so the layer stores nothing at all while looking like it worked. There is a case for the right order and a case for the wrong one. **The default for what may be cached is narrow on purpose, and it is the part worth reading before using the layer:** only `temperature: 0`. An *unset* temperature is deliberately not the same thing — the mainstream providers default it above zero, so a request that never mentioned it is a sample of a distribution, and serving the first sample forever is not a cache but a bug. **A mutation pass of 41 defects over five renderings found two gaps, and both were tests asserting in the wrong direction rather than asserting too little.** The cache key drops the signal, and the case for it compared two requests carrying *different* `AbortSignal` instances — but a signal has no own enumerable properties, so both serialise to `{}` and keeping it in the key still hit; the case now compares a request with a signal against one without, which is the comparison that can fail. And the check that a stored answer is not replayed for a streamed call was only ever exercised in one order, generate-then-stream, so the mutation that hands stored *parts* over as an answer survived; the reverse order kills it, and the symptom it prevents is an `undefined` surfacing several frames from where the mistake is. **One claim in this pattern is compiler-enforced rather than test-enforced, and saying so was worth more than a test that looked like it covered it.** A chain that awaited a layer's request transform outside the generator cannot typecheck — it would return `Promise<AsyncIterable<Part>>` where an `AsyncIterable<Part>` is wanted — so laziness is not something a suite has to defend. What the suite pins instead is the observable half: a layer has not run and no request has been sent when `stream` returns, only when the first part is asked for. **And reading the emitted output found what green verification did not, for the fifth time in this catalogue.** `joinLines` drops a blank part by design — that is what makes a conditional section safe there — so a function body assembled line by line arrived as a wall of statements with every paragraph break silently removed, and Prettier preserves blank lines rather than inserting them. `render/helpers.ts` gained `codeLines` and a `BLANK` marker: a part that rendered to nothing is still dropped, only `BLANK` produces a gap, and runs of them collapse — so the blank-line drift guarantee is kept by construction rather than by filtering. The pattern's own doc comments were reattached in the same reading pass: prose describing a layer had landed on its options interface, where it documents the wrong thing. **`context-budget` closed the group, and it is the one pattern here whose whole subject is arithmetic — which makes the interesting failures the ones where the arithmetic is right and the request is still rejected.** Three costs are not messages and come off the window before anything is measured: the room promised to the reply, the system prompt, and the tool schemas. A conversation weighed against the whole window fits right up to the moment the model tries to answer, which is a rejection on the way back rather than on the way out, and the emitted suite pins each of the three by fitting the same conversation twice against the same window with one of them changed. The system prompt is charged one message's framing even though this port sends it as a field, because one mainstream format puts it on the wire as the first message and paying for framing it may not have is the harmless direction to be wrong in. **The subtlest cost in the file was found by reading the emitted output rather than by any test, and it is the summary's.** A summary travels in the system field, so a conversation with no system prompt has not paid for that field's framing — and the summary is what brings the field into being. Charged on the way out, two turns and a summary come to exactly the window and arrive one token over it, which is the exact failure mode the pattern exists to prevent, reported by the provider from a stack that has nothing to do with summarising. The framing is now charged whenever `reserveForSummary` is above zero, for the same reason the summary's own room is reserved *before* the turns to summarise are chosen: a cost discovered after the decision it should have informed is a cost that overflows by its own size. **Nothing here shortens a message, and that is a design claim rather than an omission.** A truncated question is a different question, and answering that is worse than declining, so a conversation whose undroppable part — the system prompt and the newest turn — is larger than the window is refused with both numbers attached. `onOverflow` chooses between an arm of the result and an error carrying the same two, because a call site with no branch to put them in should not be made to invent one. **Two selection rules are the ones a hand-written sliding window gets wrong, and neither announces itself.** The search stops at the first turn that does not fit rather than stepping over it to reach a smaller one behind it: a conversation with a hole in it reads to the model as though those turns never happened, which is worse than one turn fewer and much harder to notice. And an assistant turn that called tools travels with the results answering it, kept or dropped whole — separate them and the provider rejects the request either for a `callId` that refers to nothing or for a call nobody answered, in both cases with the window arithmetic entirely correct. Pairing is by the `callId`, not by adjacency, and the rule is *every* result rather than some: a message carrying one result for this turn and one for a call nobody made belongs to neither turn, and absorbing it would pin an unrelated result to a turn that has nothing to do with it. A tool message is also charged one framing per result rather than one for the message, because this format keys a result to the call it answers and a message carrying three results is three messages by the time it is sent. **The one finding that only the strictness sweep could produce was an overload-resolution failure, and it is worth recording because it is not about narrowing.** With `strict` off, `content` on a union of two message roles is a union of two array types, and `reduce`'s overloads then resolve to the non-generic one — so the accumulator is inferred as a message part, `total + count(…)` is an error, and all twelve `toolPairing: true` combinations fail to compile while every one of them passes under `strict` and `strictest`. A `for` loop sidesteps overload resolution entirely and the reason is in a comment where the loop is. The process lesson is the same one T088g recorded and it recurred anyway: `verify-combinations.ts` checks one strictness unless told otherwise, so the loose and strictest sweeps belong *before* the gate rather than in it. **A mutation pass of 41 defects over five renderings found three survivors, and all three were fixtures hiding the thing they were built to expose.** Every case counts one token per character and turns the framing charge off, which is what makes the numbers arithmetic rather than approximation — and it also meant that nothing in the suite ever charged the system prompt its framing, so removing that charge survived; one case now leaves the framing on. The case proving the summariser is shown exactly what was dropped reserved *zero* for the summary, so the selection before the reserve and the selection after it were the same selection and there was nothing to get wrong; with a real reserve, showing the summariser the turns dropped before its own room was taken out is caught. And every pairing case used a result whose call was either present or entirely absent, which is precisely where `every` and `some` agree, so the mixed message the rule exists for went untested. **One mutation broke the harness rather than the pattern, in a way specific to mutating a generator:** a replacement containing a template literal was interpolated by the generator's own template literal, so the source stopped parsing and the mutant reported as broken rather than as caught. The lesson is that a mutation's replacement text is code inside code, and anything with `${` in it has to be spelled for the outer layer. Two smaller decisions. `reserveForHead` and `summarise` have no defaults, and the emitted `*.test-d.ts` asserts that a budget omitting either is refused: a default for the first makes `middle-out` the plain sliding window it was chosen over, silently, and a default for the second makes a summarising budget one that drops the turns instead. And the port's messages are declared here rather than imported, then compared to a written-out copy of the port's own types in both directions — a file that had drifted out of shape with `chat-model-port` would behave identically in its own suite and fail at the call site.
- [x] T116 Extend `src/engine/verify/platform-types.ts` with the `fetch` family — `fetch`, `Response`, `Headers`, `URL`, `URLSearchParams`, `RequestInit` — as narrowly as the existing timer and `AbortController` declarations. Hand-written for the reason T113 established: resolving real `@types/node` would make a bundle's verification depend on what happened to be installed beside the server. Two deliberate departures, both recorded in the file: `json()` returns `unknown` where `lib.dom` returns `any`, which is stricter here and therefore portable everywhere; and `Request` is absent because nothing generated constructs one
- [x] T117 Add `llm-orchestration` to `CATEGORIES` in `src/engine/catalog/schema.ts` and regenerate `data/schema.json`. Blocks T088i. Done. Smaller than it looked: `CATEGORIES` has exactly two consumers, the enum the shard schema is built from and the check in `load.ts` that a shard's file name is a known category — and that second one only constrains the names a shard *may* have, so a category with no shard yet is legal and nothing else needed touching. `data/schema.json` is emitted rather than written, and `schema:check` re-emits and compares, so the regeneration is verified rather than trusted.
- [x] T119 Stop the harnesses recording a verification failure as an expected refusal. `VerificationError` extends `EngineError`, and both the golden harness and the diff-stability harness caught that supertype to mean "the request was refused" — so a pattern whose own generated tests failed was written down as the expectation and the suite went green. `repository` at `pagination: offset` sat in the repository that way, with twelve snapshots recording the failure, and the offset goldens went from six-line refusal stubs to ~1000-line bundles once the harnesses were corrected to catch `CorrectableError` instead. The lesson generalises past these two sites: a refusal is a judgement about the request, a verification failure is a defect in us, and any code that conflates them turns the suite into a record of what is broken
- [x] T120 Fix the `repository` offset-paging test, which asserted `[a, b, b, c]` was `[a, b, c]` and was the defect T119 had been hiding. The window for the second page was the literal `offset: 1`, which has to agree with `limit` and silently stops doing so; it is now spelled `offset: first.items.length`, the offset dialect of "carry on from the page I am holding" that `after: first.cursor` already says for cursors
- [x] T121 Add `test/conformance/strictness.test.ts`: every pattern × every documented combination, verified at every `strictness` the tool advertises (FR-025). The golden suite only ever generated under the defaults, so two of three advertised settings were unchecked, and the gap was not theoretical — it found four patterns whose narrowing a project with `strictNullChecks` off rejects, plus `retry` and `gateway` failing under `strictest`. Only `CorrectableError` is treated as a refusal here, for the T119 reason: catching `EngineError` would have made the suite pass against the bug it was written for
- [x] T122 Narrow boolean-literal discriminants by comparison, not by truthiness, in every generated bundle. `outcome.ok === false` narrows at every strictness; `!outcome.ok` and `outcome.ok ? … : …` narrow only when `strictNullChecks` is on and fail *silently* to otherwise, leaving the union unnarrowed so that reading `error` off it is an error in the caller's build. Fixed across `builder`, `factory`, `adapter` and `gateway`, and stated on each outcome type's doc comment so a reader who tidies it back to `!` learns why not. `result` and `repository` were already immune, narrowing through type-predicate guards
- [x] T123 Make optional members of generated options types accept an explicit `undefined`, so that forwarding a value a caller may not have does not require them to build the object conditionally. `retry`'s policy overrides are now a mapped type rather than `Partial<Policy>`, since `Partial` makes a field optional without making it accept `undefined` and `exactOptionalPropertyTypes` then rejects the ordinary way of passing one through. Where the target type is the *host's* and cannot be widened — `RequestInit` in the `fetch` transport — the key is omitted by a conditional spread instead
- [x] T124 Extend `test/conformance/` past `strictness` to every conventions axis. `strictness.test.ts` became `conventions.test.ts`, which reads the axes and their values *off the schema* rather than listing them — `ConventionsSchema.unwrap().shape`, keeping a field whose values enumerate and refusing to start if it meets one that neither enumerates nor appears in `NOT_AN_AXIS` — so a new setting, or a new member of an existing one, is covered without anyone remembering to add it. Six axes × eight patterns × every documented combination is 2,414 verifications, and all five newly-covered axes passed first time: `strictness` was the only one hiding anything. The known limit is deliberate — one axis moves at a time, every other left at its default, so a defect needing two non-default conventions *together* still escapes. The cross-product is around four hundred settings per combination and is not worth its runtime. Cost: `pnpm check` went from roughly 40 seconds to a little over two minutes, which is the price of the claim and cheap against what the last silent-harness bug cost. Note the test *count* fell (899 to 805) while coverage rose sharply: 142 fine-grained strictness cases became 48 that each carry a whole axis, and a failure names the pattern, the setting and the combination
- [x] T125 Resolve the unreachable `"jest"` branch every pattern carries. All sixteen pattern modules select a test-framework import with a `conventions.testFramework === "jest"` arm importing from `@jest/globals`, and the schema permits only `vitest`, `node-test` and `none` — so the arm is dead in every one of them and has been copied forward sixteen times. Found while sweeping `typestate` across the framework axis by hand, which reported `jest` as not accepted rather than as untested; the conformance suite reads its values off the schema and so was right to never try it. Either add `jest` to the schema, which means the `expect` shim question does not arise for it but the golden and conformance costs multiply, or delete the arm from all sixteen. Deleting is the presumption: nothing has asked for Jest, and a branch that cannot execute is a claim of support that is not there. Not fixed alongside `typestate` on purpose — removing it from one module would leave that module inconsistent with fifteen others, which is worse than the dead code. Done, and the premise recorded above was wrong in the way that mattered: the schema does permit `jest`. What makes the arm unreachable is `assertExecutableTests`, which refuses the value whenever a suite would be emitted — the sandbox has no `node_modules`, and Jest's globals cannot be supplied by a resolvable package the way Vitest's can, so serving it would emit a suite that never runs (Principle III) and substituting Vitest would betray the caller's stated convention (Principle IX). Recognising the value and refusing it is therefore right, because it buys an error that names the three frameworks that work instead of a generic enum complaint; the defect was only ever the sixteen copies of a branch downstream of that refusal. So the arms are deleted and both legitimate mentions stay. Two things made this safe to do quickly rather than carefully. The golden and determinism suites passed with zero snapshot changes across all 283 tests, which is not a formality but the proof of deadness — a single changed byte would have meant the branch was live somewhere. And the refusal itself is now pinned by two cases in `generate.test.ts` rather than by a comment, because the deletion makes those sixteen modules depend on it: relax the guard and they would silently emit Vitest imports for a Jest caller, which is precisely the fidelity failure the guard prevents. The conformance suite already covered the other half — `jest` with `includeTests: false` is the only combination in which the value reaches a pattern module at all, and it still generates.
- [x] T118 Fix `joinLines` so a blank line *inside* a part survives. It flattened every part to lines and filtered the blank ones, so every function body assembled through `documented` arrived with its paragraph breaks removed — and Prettier preserves blank lines rather than inserting them, so nothing downstream put them back. Found while reviewing `gateway`; the fix improved all five existing patterns and rewrote 48 golden snapshots. Blank lines at a part's edges are still stripped, since those come from how a template literal was written. Paired with a rule the new patterns follow: a conditional block whose first line is a comment must be assembled as a list member, never interpolated, or `dedent` strips the newline above it and the comment becomes a trailing comment on the previous line — which the reflow step skips on purpose, so it also stops being held to the print width
- [x] T126 Add `test/conformance/foreign-project.test.ts`: every bundle taken out of this repository, compiled against real `@types/node` under a `tsconfig.json` written from scratch, and executed by Node's own resolver. Verification is hermetic by design — `lib: ["es2022"]`, no `node_modules`, hand-written declarations for `node:test`, `node:assert/strict` and the platform globals — which is what makes "these tests passed" mean the same thing on every machine, and also means a bundle is only ever proven against *our* account of the world. Nothing else in the gate could see the gap, and the gap was two live defects shipping in every `node:test` bundle. **The first is the one that indicts the doctrine.** `platform-types.ts` states the rule in its own header — be stricter than reality, since that is the only safe direction to be wrong in — and the `node:assert/strict` declaration beside it broke the rule: `throws` was declared as one signature with an *optional* matcher, where `@types/node` has two overloads and requires the matcher in the one that takes it. So the emitted `expect` shim forwarded a `RegExp | undefined`, which typechecks against our stub and is `TS2769` in the caller's repository. Every pattern emitting a `node:test` suite was affected, all green. The declaration now mirrors the real overloads and the shim branches on the absent matcher, and tightening the declaration *first* was what proved the gate could see it — generation failed with the same `TS2769` before the shim was touched. **The second is invisible one layer deeper down.** Seven patterns hardcoded `import { expect } from "./expect.js"`, each having rewritten by hand the helper in `expect-file.ts` that exists to get this right — and whose doc comment already warned about the neighbouring version of this bug. The sandbox transpiles a bundle and runs the emitted `.js`, so `./expect.js` resolves there and raises `ERR_MODULE_NOT_FOUND` in a project spelling its imports `./expect.ts`, which `importExtensions` advertises. All seven now route through `frameworkImports`, which deletes six near-identical local `frameworkImport` helpers; `result`'s also interpolated the framework name straight into the specifier, so `testFramework: "none"` would have emitted `from "none"` had that value not suppressed the test file entirely. Cheap coverage for the same class went into `conventions.test.ts` as `specifierDrift`, which holds every sibling specifier in every file to the caller's convention across all six axes and would have caught all seven at no measurable cost — `coreModule` exempted, since that is the caller's own path and imposing our convention on it would rewrite the one specifier they gave us verbatim. **The unused-symbol checks are scoped by file role rather than applied to everything, and the line is drawn on what a caller integrates.** `noUnusedLocals` over a `core` or `binding` file found `debounce` declaring a burst flag its trailing-edge rendering assigns twice and reads never; over examples it found four importing helpers they never demonstrate, which is a defect on its own terms and is fixed in `adapter`, `branded-type` and `discriminated-union` — the `branded-type` one revealing that the `cast` rendering's example never constructed a value at all, since the unsafe constructor is the only way in there and it was imported and unused. `specification` still over-imports for two of the three files sharing one list, and a `.test-d.ts` is deliberately held to the looser standard: a file that declares types in order to assert things about them has unused declarations as its purpose, and satisfying the flag would mean adding `void` references that say nothing to a reader. Two notes on the harness itself. `--experimental-strip-types` rejects parameter properties and enums, which are ordinary TypeScript a caller's build compiles happily, so it failed eight patterns for a `constructor(private readonly store: Store)` before being switched to `--experimental-transform-types` — a limit of the runner reported as a defect in the code. And the pinned compiler has no in-process JavaScript API, so this drives the CLI the way the engine drives the same binary, which is closer to the claim anyway: it is the command a caller types
- [x] T130 Make two bundles safe to unpack into one directory. Found by driving the built server over stdio from a project outside this repository — an agent's actual sequence: list, describe, generate `repository`, `retry` and a branded `OrderId` into one `src/`, hand-write the domain types, build with the project's own `tsc`. It failed with 18 errors, none of them in code the generator had been asked about. Every pattern emits the `node:test` shim at `expect.ts` and each emitted a *different* file there, so the three overwrote each other and whichever landed last decided which matchers existed; the repository suite then compiled against a shim carrying `toBe` and `toEqual` alone. Two independent causes, and the second is the one worth remembering. The shim was tailored per pattern, each naming the matchers its own suite called, on the reasonable ground that an unused matcher is unexercised code shipped to a caller — reasonable per bundle and wrong across bundles. Underneath that, the provenance header would have kept the files apart even with identical matchers, because it names the pattern and the options hash, so `expect.ts` differed by request for a file whose bytes have nothing to do with the request. **The whole class was invisible to the gate for one structural reason: a bundle is verified alone, and each of these was correct alone.** Fixed by making the shim canonical — one file, the full matcher set, the rejection mirror always — and by giving `RenderedFile` a `provenance: "shared"` marker that swaps the per-pattern header for one naming no pattern, which is what actually makes the bytes coincide. Colliding writes are now no-ops. The guard in `foreign-project.test.ts` asserts the invariant rather than re-enacting the symptom: a path two patterns both emit must carry identical bytes, which fails for any future shared file carrying anything request-specific, the header included. It is two cases because the invariant and the demonstration have different natural scopes — agreement is swept across all 26 patterns, since the rule is about *any* two and a hand-picked list would leave a pattern added later unguarded, while compiling and running together stays at four because that half spends compiler and Node processes. Sweeping is affordable only because generation is the cheap part: 26 bundles in about five seconds, against the minutes the spawns cost. Verified by undoing the fix and watching the sweep fail and name all 26. A scratch driver, `scripts/check-collisions.ts`, reports the inventory the test only asserts over, and it is the reason the sweep exists: 26 patterns emit 91 distinct paths and share exactly one, so the four-pattern list was covering the only case that exists today and would have silently stopped doing so. FR-020, Principle IV's reuse bullet and guarantee 4 in `contracts/mcp-tools.md` all said "every file names its pattern" and are amended; the tool contract gains the promise this establishes, that bundles compose in one directory, since that is what a caller depends on and had no way to know
- [x] T131 Close the `identifiers` half of a request, which was open while `options` was closed. Found by driving the built server the way an agent would: `describe_pattern` publishes every option, its values and its default, and said nothing about identifiers — so the name to pass was guesswork, and `{ entity: "Order" }` was the guess. It was accepted by all twenty-six patterns, read by twenty, and dropped by six, which emit one module named after themselves. Dropped, not rejected, is the expensive part: the call read as success, and the name still entered `optionsHash`, so those six returned byte-different provenance headers over a name appearing in none of their files. Two callers wanting the same thing got different bytes, which is Principle I failing quietly. Fixed by declaring the roles in the catalog — a name and a description each, twenty patterns with `entity`, six with none — refusing an undeclared role in `resolve.ts` with `UnknownIdentifierError` the way an unknown option is refused, and publishing them through `describe_pattern` in both halves of its output, including an explicit "None: this pattern emits one module named after itself" rather than an omitted section, because a missing section reads as an oversight and invites the guess again. The refusal names the declared roles so the correction needs no second call. Role is checked before value, for the reason a name is checked before its value everywhere else: a caller told their key is wrong has no use for a complaint about the string they put in it. The conformance sweep asserts the declaration is *true* rather than merely present — every declared role must change the generated code and the name must appear in it, since a difference confined to the hash is the original defect wearing a disguise — and every undeclared role must be refused. Sweeping all twenty-six is the point: a declaration is per pattern, so a sample says nothing about the next one added
- [x] T132 Stop a caller's choice of noun from breaking the code it names. Two defects, both found by T131's sweep rather than by reasoning, and both invisible to a gate that pins one noun. **`Order` is five characters, and length is not inert.** It decides where Prettier wraps, and a wrapped statement can carry a `@ts-expect-error` away from the line it was written to assert: the directive then suppresses nothing and the escaped error is reported, so the pattern fails its own verification — two errors for one mistake, on a line the caller never sees. `discriminated-union` broke at `Voucher`, seven characters. Widening to the 64 the validator permits found two more sites in `typed-emitter` and one in `typestate`, which is why the guard sweeps the cap rather than a middling name: the longest a caller can send bounds every name they can send, whereas a name picked for looking long proves nothing about the ones either side of it. Fixed by moving each directive next to the token that errors — inside the object literal above the offending property, inside the argument list above the offending argument, between the `=` and an initialiser — which also stops the formatter collapsing the construct, so the shape holds for a short name too. The generated files explain the placement, since the obvious tidy-up is to hoist the directive back above the statement. **And a name whose plural could not be derived was silently dropped.** `deriveNames` refuses one it cannot pluralise confidently, states the rule and says to supply a different name — all correct, and `generate` caught it with a bare `if (result.ok)` and no else, falling back to generic names. A request for a `Staff` repository returned `EntityRepository` and said nothing: the same silent-ignore defect as T131, at the next stage of the same pipeline. Made a refusal, which then exposed why nobody had noticed it was silent: the `-on` rule, written for `criterion` and `phenomenon`, matched every `-ion` noun and so refused `Subscription`, `Transaction`, `Session`, `Notification`, `Permission`, `Version`, `Connection`, `Collection`, `Region`, `Option`, `Action`, `Question` and `Division` — sixteen of fifty-two common domain nouns, thirteen of them for no reason. Surfacing the refusal without narrowing the rule would have turned a silent degradation into a hard refusal for most of a realistic vocabulary. `-ion` is now excluded, which costs none of the words the doubt was aimed at because they are all exception-table entries and the table is consulted first; one refusal remains, `Staff`, which is genuinely ambiguous. Both halves are pinned, including that an ordinary `-ion` noun is *not* refused — the narrow rule and the visible refusal have to hold together, or the product is worse than before
- [x] T133 Stop the harnesses sending an identifier no pattern reads. Six golden suites, the diff-stability sweep, the budget sweep, the collision guard, the conventions sweep and two scratch drivers all passed `entity` unconditionally, which was harmless while it was ignored and became a refusal once T131 landed — 132 failures, all of them the harness rather than the product. Replaced with one `goldenIdentifiers(pattern)` helper in the golden harness that supplies the declared roles and nothing else, so a pattern added with a different role needs no harness change. Two of the call sites took a pattern *name* and now take the entry, which is the small cost of the request being honest. Worth recording that the conventions sweep would not have failed loudly: it reports a refusal as an outcome, so every convention would have looked inapplicable to those six rather than untested. The regenerated snapshots are the six patterns that read no identifier plus the three whose directive placement moved, and nothing else, which is the check that the change was scoped to what it claimed
- [x] T134 Stop inventing plurals for words that have none. Found by noticing one wrong answer in T132's output — `Aircraft` pluralising to `Aircrafts` — and worth the audit rather than the one-line fix, because the mechanism that produced it is a *list*, not a rule, and a list of seven against a class with scores tells you the answer before you look. Forty-seven of forty-eight candidates were pluralised confidently and wrongly: `Corps`/`Corpses`, `Middleware`/`Middlewares`, `Analytics`/`Analyticses`, `Headquarters`/`Headquarterses`, `Data`/`Datas`. **This is the worse of the two naming failures and the one that was invisible, for the same reason each time: the default rule is confident about a `-t` or an `-s` ending and has no way to know the word is a collective, so nothing was refused and nothing looked wrong.** T132's refusals at least said so. Fixed by growing the list to forty-nine, chosen by group rather than by hunting words — the `-ware` and `-craft` compounds, the `-ics` field names, the singular collectives ending in `-s`, the mass nouns — and deliberately not exhaustively: `Bandwidth` pluralises legitimately and `Feedback` is split by usage, so both are left alone, and `Innings` is left as a word no software domain reaches for. Widening `-s` into a doubt instead was considered and rejected, since it would refuse `Address`, `Class`, `Process` and `Status`, all far commoner than the collectives it would catch. One ending needed the doubt rather than either table: `-ics` names a field, which is invariant, and is also how an `-ic` noun pluralises, so `Mechanics` is both at once while `Topics` and `Metrics` are only plurals — refusing tells a caller who sent a plural by mistake what they did, where the previous answer was `Topicses`. Checked before changing anything that an invariant noun is safe at all, because `pluralEqualsSingular` is computed and no pattern reads it: all twenty identifier-taking patterns generate, compile and pass for `Corps`, `Data`, `Middleware`, `Aircraft` and `Analytics`, the case being distinguished by casing and by file-stem suffixes rather than by the flag. The guard names five words rather than only sweeping the list, since a sweep over a list is nearly a tautology and would pass just as well with the entries deleted; verified by deleting two and watching three tests fail. It also pins that no word sits in both tables, where the irregular plural would silently win, and the pre-existing lowercase check earns its place here — a capitalised entry would never match the lookup and would fail as a silent wrong plural, which is exactly this defect
- [x] T135 Case a caller's noun from its words rather than from one of its characters, and collapse a repeated noun wherever the two names meet. Found by continuing T132's method along the axes it had not varied: T132 widened `Order` to the longest name the validator takes, which is *length*, so the sweep here varied *shape* — word count, acronyms, digits, snake and camel input, names already plural, and names identical to a pattern's own vocabulary — driven over the built server rather than the engine, since that is the surface an agent has. Four defects, three of them silent. **Two templates cased names by hand, and were wrong in both of the ways available.** Lowercasing the whole subject runs the words together, so a `WebhookEvent` request exported `webhookeventId` and titled a generated test "accepts a well-formed webhookevent"; lowercasing only the leading character mangles every acronym, so an `APIKey` request exported a factory named `aPIKeyId` and a `HTTPRequest` repository one named `hTTPRequestId`. Both compiled, both passed the tests they shipped with, and neither is reachable with a single-word name, where `camel`, `pascal` and the subject coincide — the golden suite pins `Order`, so it agreed with both spellings. Fixed by deriving the value form in `withNoun` from the split words, deleting the two hand-cased sites, and giving the two templates that also put the subject into prose a spaced form for it, so an error message reads "this webhook event has already left" rather than either the run-together word or a camel identifier. **The rule that gave `OrderId` instead of `OrderIdId` compared the noun against the whole tail, so it missed every partial overlap.** `Event` is the likeliest subject anyone gives an emitter and it produced `EventEvents` and `EventEventName`; `Parser` gave `ParserParser`. The longest overlap now wins and a noun matching the name's plural counts as the same word, with the plural's spelling surviving there and the entity's everywhere else, so `OrderID` keeps its acronym. One pattern is excluded on evidence rather than taste: collapsing a `State` subject in `typestate` made the state union collide with the class named after the subject, four compiler errors, so it keeps `StateState` — the tidier name did not compile, which is the whole argument. **And a name that is already plural was pluralised again.** T134 rejected widening `-s` into a doubt on the grounds that it would refuse `Address`, `Class`, `Process` and `Status`; three of those end in a double `s`, which is not doubtful, and the fourth is `-us` and already belonged to the Latin rule — so the premise was wrong and the case it protected was not the one that mattered. `Orders`, among the likeliest things anyone hands a repository, produced `orderses` as the *collection* name, so the invented word would have reached a schema rather than a type. A bare `-s` is now doubtful, `-ss` exempt, and the genuine singulars left over — `alias`, `lens`, `canvas`, `gas`, `bias`, `atlas` — are table entries like every other word with one right answer. The same sweep found `Quiz` answered `Quizes`, which is a missing rule rather than a doubt: a single `z` after a vowel doubles, and `Waltz` and `Buzz` are untouched. Guarded by widening the identifier conformance sweep to a multi-word name and an acronym across every pattern, *with the generated tests*, since one of the two spellings only ever appeared in a test title; verified by reintroducing each defect and watching the sweep name the pattern. The table's redundancy check had to be rewritten to ask the deriver instead of restating the rules, because it restated them and drifted: the six new entries read as dead weight to it while being the reason the doubt costs a caller nothing. Two things this did *not* find, recorded because the absence is the useful part: 190 requests covering every declared option value under both adversarial names produced nothing, so the mis-casing was not confined to a branch the defaults miss, and the file stems were clean throughout — a stem is derived apart from the identifiers inside the file, and only the identifiers were wrong
- [x] T136 Say which identifier was refused. Found while reading the refusals a name sweep produced: every reserved name came back as `Identifier "entity" is not usable as a generated name: entity the supplied value is reserved and cannot be used as a generated name` — the offending value absent, the *role* quoted where a reader expects the value, and the opening clause repeated at the end. A caller holding that cannot tell which of the names they sent to change. The cause is worth the entry: the MCP adapter rebuilds every other message from the error's structured fields, on the stated ground that passing `error.message` through would make an engine wording change a silent change to what a model receives — and this one branch passed `error.message` through a filter that strips quoted spans, which took the value with it. The filter's reason was sound. A value that fails the identifier charset can hold prose, and prose in a tool result is an instruction to whatever reads it next; the module already has `safe`, which quotes an inert value and describes anything else, and a value that reached the reserved-name rule has by then passed a charset stricter than `safe`'s own test. So the fix is to carry the value and the rule as fields — `InvalidIdentifierError` gains both, and `checkIdentifier` and the plural deriver return the constraint free of the value as well as the sentence that names it — and to compose the caller's message here like every neighbour. The subject is chosen rather than interpolated, because "Identifier the value you supplied cannot be used" is not a sentence. The filter is deleted rather than left unused. Pinned in `error-hygiene.test.ts` in both directions, which is the only way this stays fixed: a reserved name must name itself and state the rule exactly once, and a name carrying an injection attempt must be withheld while the rule that would fix it still arrives
- [x] T137 Keep a pattern's noun in the file it writes, so two bundles cannot claim one path. Found by asking what T135's collapse changed besides type names: it changed *stems*, and a stem is a path, so the T130 invariant — a path two patterns both emit must carry identical bytes — needed rechecking under names other than the one it was written against. It failed for five. `typestate` named its file after the bare subject while every other pattern appends its own noun and collapses the repetition when the subject already ends in it, so a `typestate` and a `branded-type` request for `OrderId` both wrote `order-id.ts`, and so did `OrderRepository` against `repository`, `OrderEmitter` against `typed-emitter`, `Result` against `result`, and `Event` against `discriminated-union` — the last of those introduced by T135, the other four older than it. This is exactly the T130 defect, reached through the caller's noun rather than through a shared support file, and the T130 guard could not see it: it sweeps all twenty-six patterns for byte agreement but pins one noun, and `Order` is nobody's noun, so no two stems ever met. Fixed in the one place the class comes from — `typestate`'s stem now carries `State`, which no other pattern appends — rather than by walking back T135's collapse, which is right for a type name and would only have hidden four of the five. The rename reads as an improvement on its own terms: the service was claiming `order.ts`, which is the file a caller's own `Order` most likely occupies, and now writes `order-state.ts`. Guarded by sweeping the agreement case over the five nouns as well as every pattern, which is cheap — under four seconds for a full sweep, since it needs no compiler — and falsified by restoring the old stem: all five fail, and the golden-noun case still passes, which is the blind spot stated as a test result. Verified last through the built server the way a caller meets it: five bundles for `OrderId` fetched over stdio, written into one directory by a write-every-path loop, compiled by a real `tsc` against real `@types/node`, and their suites run by Node — twenty distinct paths, nothing overwritten, no errors, five suites passing
- [x] T138 Hold type-level assertions in place at any width a caller formats to, and refuse the widths where they cannot be held. Found by asking which caller input had never been varied at all: `prettierConfig` is excluded from the conventions sweep because its value space is open, and that exclusion is the argument that file makes about itself playing out — an untested setting looks supported. Print width is the field within it that moves every line, and moving lines is what T132 was about: a `@ts-expect-error` asserts about the line below it, so a width that wraps that line carries the assertion off the token that errors, the directive is reported unused, *and* the error it was suppressing escapes — two failures from one displacement. `chat-model-port` and `model-middleware` broke at 40 on directives correctly placed at every width previously tried. Fixed by keeping the object literal expanded so the directive sits on the property that errors rather than on the line the property started on, which is stable at 40 through 200 because Prettier preserves an object broken in its source. Guarded by adding print width to the conventions suite as an explicit case at 40, 80 and 120 — Prettier's default, the commonest widening, and the margin — and falsified by restoring one directive, which fails at 40 alone. Below 40 the same displacement returns in `discriminated-union`, `specification` and `unit-of-work`, all in example files, and that is a support question rather than a defect: 40 is already half of Prettier's default, so the floor is stated and narrower is refused as the caller's to correct (FR-049). Worth recording why the refusal needed work of its own: `FormatConfigError` covers both an option that cannot be set and a value a settable option cannot take, and said the same way the second contradicts itself — the caller was told `printWidth` cannot be set here beside a list naming `printWidth` as configurable. The reason is now carried as a field, since the adapter rebuilds messages from fields rather than passing `error.message` through, and the value case omits the list, which is not what that caller needs
- [x] T139 Verify a binding against machinery in another directory, which is where machinery actually lives. Found by sweeping the last input that had only ever been given one shape: `coreModule` is the single caller string written verbatim into generated code, and every test passed it a plain package name or a `./` sibling. Fourteen shapes through the built server established that the guards hold — an injection attempt, a traversal, a builtin, an empty string and a whitespace string are all refused, and a scoped package, a subpath and a nested relative path all reach the emitted import unchanged — and that one legitimate shape was refused with them: `../lib/core.js`, which is the only specifier a binding in `src/orders` can write to reach a core in `src/lib`. The cause is that verification regenerates the core at the path the specifier resolves to, and from a bundle written at the sandbox root a climb resolves above the root, where `assertSafePath` refuses to write. So the ban was a consequence of a placement choice rather than a decision about specifiers, which is what its own comment said and what research.md did not record — and the refusal listed the shapes it accepted without saying the climb was the problem, leaving a caller unable to tell that their specifier was right and their layout unsupported. **The split exists for a project that keeps machinery in one place and bindings in another, and the one layout it could not verify was that project.** Fixed by placing the bundle as many levels down as the specifier climbs, so the resolution lands inside the root and `..` needs no special case; interior `..` stays refused, since it is a longer spelling of a path that has a shorter one, and the climb is bounded at four, which is past any real layout. Both halves falsified separately, because either alone would have looked fixed: restoring the ban fails the two climbing cases at the validator, and keeping the ban lifted while flattening the placement fails them at the compiler. Guarded where it will be maintained rather than only where it was found — the climb is now a third split combination in the golden harness, so it flows into the golden suite, the conventions sweep, the composition guard and the foreign-project suite at once. Confirmed the caller's way last: a `core-only` request placed in `src/lib`, a `binding-only` request placed in `src/orders`, one `tsc` over both, clean. The three catalog descriptions now name the climbing form too, since `describe_pattern` is the whole of what an agent knows and it had no reason to try a shape the documentation did not mention
- [x] T140 Attribute a file by what shapes it, so a second request to one pattern cannot rewrite the first's machinery. Found by asking what the composition guard assumes: it compares *different* patterns, on the reasoning that two patterns deriving one stem is what collides. A second request to one pattern collides the same way and was never checked — a split pattern hands back its machinery with every `full` request, so a project with two repositories has been sent `repository-core.ts` twice. Three pairings through the built server showed all three disagreeing: two entities, the same request asked as `full` and as `core-only`, and the same request with and without tests. The code was identical every time; the header was not, because the hash covered the whole request including the entity the machinery is defined not to know. **A file that varies with an input that cannot reach it is not attributed, it is stamped** — and the cost was more than an overwrite: the marker exists so an agent can compare an installed core against what a new binding needs without compiling (research.md §11), and two cores generated under identical options disagreed. Fixed by hashing, for the machinery, only the options that can shape it — dropping the identifiers and the emission-only options — and by dropping `includeTests` from every file's marker, since it decides whether a suite exists rather than what any file says. The first attempt keyed that on the `core` role alone and was wrong in a way worth recording: twenty-three patterns call their principal module `core` without splitting anything, and for them that module *is* the caller's type written out, so the rule stripped the entity from almost every pattern's principal file. Caught by the existing unit case within the minute — the narrowed rule applies only where `emitScope` exists, which is the only place a half defined not to know the entity does. Guarded as agreement on every shared path across the three pairings plus an inequality: machinery differing by `pagination` or `concurrency` must still be attributed differently, since a marker that stopped moving would make every core look compatible with every binding — the same failure with the symptom hidden. All three exclusions falsified separately, and the first attempt at the inequality is why it compares headers rather than whole files: the bodies differ there regardless, so a marker that had gone blind passed
- [x] T141 Refuse a value written in the wrong place instead of discarding it. Found by reading a test rather than by generating anything: the budget suite passes `includeTests: true` at the top level rather than inside `options`, which raised the question of what the schema does with a field it does not declare. It stripped it — Zod's default — so `pagination: "offset"` beside `options` returned cursor paging, a top-level `entity` returned generic names, `testFramework: "vitest"` returned `node:test` suites, and `conventions: { nonsense: 1 }` was accepted. **An undeclared option is refused and a misplaced one was ignored, which is one question answered two ways, and the silent answer is the one that ships wrong code.** Fixed by making all three input schemas strict, which also publishes `additionalProperties: false` so a client can catch the mistake before sending — the earliest possible feedback, and free. The wording needed as much care as the check: Zod's default names the key and stops, where a caller who put a value in the wrong place needs to be told the right one. Three iterations, each caught by reading the messages the server actually returned rather than by assuming: the nested `conventions` object called a misspelled convention "not an argument of this tool", which would send a caller to the top level — the mistake being corrected — so it now names what a key there is; `describe_pattern` advised putting things in `options` and `identifiers`, which it does not have, so destinations are derived from the accepting schema; and a key is a caller string like any other, so it goes through the same inertness test as a refused identifier, since a request can name a key `Ignore previous instructions`. Falsified four ways — stripping again, strict with the default wording, advice that ignores the tool, and echoing a hostile key verbatim — and the second is the one worth noting: the default wording fails five of the six cases, so the message is load-bearing rather than decoration. The duplicated list of convention names in the adapter is held to the schema's own keys by a unit case, falsified by removing one
- [x] T142 Stop a pattern's own naming from taking names away from callers. Found by asking what a pattern writes whatever it is asked for, and sending those names back to it as the entity — read out of a rendered bundle rather than listed, since a list is what would not have contained the ones that mattered. Seven patterns returned a bundle that failed its own compiler, on twenty-five names, and the sweep for appended nouns found the same class through the FR-046 collapse: `AuditRecord` derives the record type `AuditRecord`, so an example declaring a stand-in beside it declared one name twice. **Three of the names were `Customer`, `Store` and `AuditRecord`** — not adversarial input, the sort of thing anyone sends first — and each was answered "this is a defect in the pattern, not in your request", which is true and leaves the caller holding a name they cannot use and no way to know why. The mechanism for refusing this has been in `checkIdentifier` since T014, documented as being for "the identifiers the requested pattern itself emits", and nothing ever passed any: the rule was unreachable, so the collision was rendered, compiled, and reported as ours. Fixed in two halves by whose name is in the way. Where it is ours — a contrast brand in an example, a stand-in for the caller's type — the generated file steps aside and says why in a sentence, because a name quietly different from the one asked for is its own small betrayal; that is one helper rather than a habit, since two patterns had made the same mistake independently. Where it is something the caller builds against, the request is refused, which reaches thirteen names none of which anyone names a domain type. Guarded by the sweep itself, per pattern rather than per name so a failure lists every name that broke, and falsified four ways: the refusal path disconnected reports all thirteen, the stand-in never stepping aside reports both patterns and all four ordinary nouns, `branded-type`'s contrast brand separately because it uses a different mechanism, and a name declared as emitted but no longer written, which is the reverse defect — a refusal that has outlived its reason and which no other test would notice
- [x] T143 Judge a name collision on the name that reaches the code, not on the spelling that was sent. Found by asking what T142's own refusal compares: it compares strings, and a caller writing `repository` is asking for exactly the collision that `Repository` is refused for. Six of the thirteen refused names had a lowercase spelling that got through — `repository`, `newRecord`, `equal`, `typedEmitter`, `id`, `expect` — each producing a bundle that failed its own compiler with the import conflicting against the local declaration, which is the defect T142 was written to remove, reachable by typing the same name in the casing someone is more likely to use. **A guard that a caller's spelling can walk around is a guard against the spelling, not against the collision.** Both sides are now compared as their derived Pascal form, which required the word-splitting and the casings to move below both the deriver and the validator, since a second splitter written in the validator to avoid the import is the drift that would let the two disagree again; `capitalise` moved with them, including the acronym-plural clause a copy would have quietly dropped. `REPOSITORY` survives the derivation as itself, so it stays a different name and is not refused for resembling one — checked, since a refusal that over-reaches is the mirror of the defect. Guarded generically rather than by the six: every declared emitted name is sent in its lowercase spelling, so a name added later is covered without anyone remembering to. The rule the caller reads now says the casing does not save them, because a refusal for a name they did not type is otherwise unexplainable. Falsified by restoring the string comparison, which fails all fourteen
- [x] T144 Look for a pattern's own names in every branch, not only at the defaults. Found by asking what T142's guard cannot see: it reads one render, so a name that exists only under a non-default option is not among the candidates it tries. Two were hiding there — `specification` writes `RefinedBy` under `composition=free` to accumulate what a list of specifications proves, and `unit-of-work` throws `KeyChangedError` under `tracking=snapshot` when a draft's key moved — and both produced the same non-compiling bundle the defaults no longer produce, reachable by choosing an option. **A guard drawn from one configuration checks the names that happened to be in view**, which is the same mistake as pinning one noun and calling the name space covered. Both are things a caller builds against — one appears in signatures, the other in a `catch` — and neither is a name anyone gives a domain type, so both are refused rather than renamed. The guard now renders every declared option value and tries each name under the branch that writes it, which also fixes the reverse check: the two would have been reported as refusals for names no longer written, since the defaults do not write them. Falsified from both sides — the declarations removed with the branches rendered, which reports both names as defects; and the branches narrowed away with the declarations in place, which reports both as unnecessary refusals — and the pair of pinned cases exists because a future narrowing of the sweep would otherwise be silent
- [x] T145 Disclose the names a pattern keeps for itself, instead of charging a turn to find out. Fifteen names across seven patterns were refusable and undiscoverable: `describe_pattern` describes every option, its value space, its default and every legality rule verbatim, and said nothing about a name that would be refused — so the only way to learn `Repository` is taken was to ask for it and be told. **The argument for legality rules being readable data is the argument for these**, and it is the same turn either way. Stated in both halves of the answer: a `reservedNames` array in the structured content, and a `**Taken:**` line in the prose, which carries the casing clause because a caller who reads `Repository` and sends `repository` is asking for the same collision and would otherwise think they had found the way round. The names live next to the templates that write them, which is what moved the module list out of the pipeline into `patterns/registry.ts`: having the catalogue's description reach through the generator to read a declaration would put the two the wrong way round, and copying them into the catalogue data would reintroduce the list that drifts. Guarded as a round trip rather than against the declarations — every disclosed name must actually be refused, and refused as the caller's to fix rather than as our defect — since asserting the description against the list it reads would pass whatever either one said. Falsified twice: disclosing nothing, which fails on the count and on the prose; and disclosing a name the engine accepts, which is the failure mode a description drifting behind a rename would produce
- [ ] T089 [P] Add the agent skill surface under `skills/patterns/SKILL.md` driving the CLI, completing the third delivery surface (Principle X)
- [x] T090 Resolve the published package name and add `server.json` with a `name` matching `package.json#mcpName`, since the registry's npm ownership check compares them exactly. Published as `patterns-mcp` on npm — the name the `bin` entry already used — under the registry name `com.perussina/patterns`, a namespace proven by a DNS record rather than by an account on a code host, which keeps the identity attached to the domain. What the task did not anticipate is how many copies of the release identity there were: the version was written in `package.json`, in the engine's public API, and in the handshake, and `server.json` adds a fourth that the registry compares against the published package's — answering a mismatch with "Registry validation failed for package", no field named and no expected value given. So the version and the name now live in `src/version.ts`, the two JSON files are compared against it by a test, and the handshake sends the registry name with a readable `title` beside it, which is the division the protocol draws between identifying and displaying. Publication is the one operation here with no useful failure message, so every check exists because getting it wrong is silent until then
- [ ] T091 [P] Generate pattern documentation from catalog data in `scripts/emit-docs.ts` rather than maintaining it by hand
- [x] T092 [P] Add a CI workflow in `.github/workflows/ci.yml` running `pnpm check`, with the engine/MCP boundary rule as a required gate. One step per job rather than a step per concern, so CI cannot reach a different conclusion than a developer did — the boundary rule and the determinism rules are lint rules, and lint is inside `pnpm check`. The matrix is 22 and 24 rather than the 20 and 22 this task named, because writing it is what surfaced T146: Node 20 cannot run this at all. macOS earns a job because the sandbox resolves symlinked temporary directories there, which no Linux runner exercises. A second job builds on a supported runtime and then runs the built binary on Node 20, asserting the refusal and the version in its message: the unit tests can only check the version arithmetic, and this checks the behaviour on a runtime that genuinely lacks the flag
- [ ] T093 Build the agent evaluation set in `test/eval/` measuring first-attempt success (SC-006) and post-refusal recovery (SC-007) against a held-out task set, both targeting 90%
- [ ] T094 Settle the open item on response rendering: compare Markdown with path-headed fenced blocks against a serialized structure using the T093 evaluation set, and record the decision in research.md
- [ ] T095 Warm the compiler and formatter at startup and add a latency benchmark in `test/bench/` confirming verification is never the dominant request cost (SC-009)
- [ ] T096 Security review against FR-032 – FR-035: no `eval` or dynamic function construction, no network calls or credentials or install hooks in generated code, all paths derived from validated inputs, all caller values escaped in messages
- [x] T097 [P] Add `LICENSE` and confirm every catalog entry's `license` field permits commercial modification (SC-012). MIT for the tool, with a clause saying the output is not covered by it: the whole proposition is that a caller pastes emitted files into their own repository, and a notice obliged to travel with a file meant to be edited as the caller's own is a cost with no beneficiary (FR-036). "Confirm" turned out to be two checks, neither of which was the obvious one. Every entry's licence is already validated against an allowlist, so no entry can carry forbidden terms — but the allowlist itself is an array, and adding `CC-BY-NC-4.0` to it would read as widening support rather than as poisoning commercial redistribution of the whole catalogue, so each term is now classified explicitly and a new one has to be classified before the gate goes green. The second is a cross-file rule, which is where the catalogue validator earns its keep: the licence tells a reader that every pattern is original, a borrowed implementation is a permitted addition, and nothing but the pair being read together can see the contradiction. Falsified both ways — an `Apache-2.0` entry and a reworded sentence each fail, with the message naming which
- [x] T111 Remove `passWithNoTests` from `vitest.config.ts` once every suite has tests, so a suite that silently loses all of its tests fails the gate instead of passing it. Removed once the `parity` project stopped being empty, which is what it had been hiding: quickstart scenario 12 said to run `pnpm test parity`, that command reported success, and the project it named contained no files — the flag turned "there is nothing here" into a pass, and the scenario read as evidence of the parity guarantee while proving only that vitest can start
- [ ] T127 Decide whether every emitted file, and not only the modules a caller integrates, is promised to compile under `noUnusedLocals` and `noUnusedParameters`. T126 draws the line at file role and fixes the integrated side plus three examples; what remains is `specification`, whose example and type-test share one import list that over-imports for both, and the `.test-d.ts` question in general, where an unused declaration is the file's purpose rather than a defect. Either extend the guard's stricter pass to the example role and give `specification` a per-file list, or state the narrower promise in the docs so a caller with those flags on knows which files to expect to trim
- [x] T146 Refuse a runtime that cannot sandbox a generated test, instead of blaming the pattern for it (FR-053). Found while writing T092's matrix, which that task specified as Node 20 and 22: on Node 20 the server does not degrade, it misattributes. Generated tests run under Node's permission model, the flag was called `--experimental-permission` until 22.13.0 renamed it, and an older runtime rejects the *flag* before reading the test file — so the child exits with `bad option`, the outcome is indistinguishable from a failing test, and every request carrying tests came back as "Generated code failed its tests. This is a defect in the pattern, not in your request" with a reference number for a bug that does not exist. Verified as a caller meets it before fixing anything: the built server on Node 20, one `result` request, that exact sentence. **The `engines` field was a promise nothing enforced**, which is how a runtime three versions below the floor got that far. Fixed in three places reading one constant: the range an installer sees, a startup refusal in the binary, and a new error class for a library caller, since a too-old runtime is neither the caller's request nor our catalogue and the taxonomy had no third answer — it was being reported as ours. Refusing at startup is deliberate over serving what such a host can still do: a request with tests omitted would succeed, and a service whose central guarantee silently does not hold is worse than one plainly absent. Confirmed in both directions on the built binary — Node 20 refuses with the floor named and exits non-zero, Node 22 serves a verified bundle — and demonstrated accidentally when a stray `PATH` left the contract suite on Node 20: fourteen tests failed with the runtime refusal rather than with fourteen bogus defect reports, which is the whole change in one accident
- [x] T147 Check the served protocol against the revision it claims, in the era clients actually use (FR-054). Prompted by asking whether the server follows `2026-07-28`, and the first two answers were both wrong. The SDK's `LATEST_PROTOCOL_VERSION` reads `2025-11-25` and `SUPPORTED_PROTOCOL_VERSIONS` does not list the revision at all, which says plainly that the pinned SDK cannot speak it — those constants enumerate the *legacy* revisions, the modern one lives beside them as `FIRST_MODERN_PROTOCOL_VERSION`, and reading them as a ceiling is how a comment asserting the fields "travel nowhere" came to sit above a declaration that was working. Probing the built binary settled it: `server/discover` answered on a connection where nothing had been sent, results carried `resultType`, `ttlMs`, `cacheScope` and the server's identity, a missing resource came back `-32602` rather than the retired `-32002`, `ping`, `logging/setLevel` and the subscribe pair were gone, and an unsupported version claim was refused `-32022` with the supported list — the whole revision, already served, and none of it asserted anywhere. **The suite could not have asserted it: the SDK's client defaults to `versionNegotiation: 'legacy'`, so every contract test spoke the older protocol** while the host this was built for speaks the newer one, and a session opened through `McpServer.connect` cannot reach the modern era at all, since the era decision belongs to `serveStdio`. So the session helper takes the era as a parameter and opens it through the real entry point, and the wire assertions for the cache fields — previously gated on a list that can never contain the revision, a skip condition indistinguishable from a pass — now run. One genuine violation, and it was ours rather than the SDK's: results were annotated with `io.modelcontextprotocol/cache-hint`, chosen so a client would not have to learn a second spelling of `ttlMs`, in a namespace the revision reserves by structure for the protocol and its official extensions. The specification defines no such key, so the borrowing bought nothing — and a name sitting where a later revision may define its own meaning stops being ignored and starts being misread. Moved to `com.perussina.patterns/`, the reverse-DNS form of the identity the package already publishes under, with the error-code keys brought along from a `dev.patterns/` prefix that named a domain nobody here owns. Guarded on the wire rather than by searching the source, since a search only finds the mistakes already imagined: every `_meta` key a client receives must be one the specification defines or one this server declares. Both eras are exercised as such, because the era is chosen by how the connection opens — which is also what one wrong assumption cost: a claim-less opening is an era-*ambiguous* request that this server answers as legacy, and reading its success as a missing rejection produced a test that failed against correct behaviour
- [x] T148 Validate the requests the documentation shows against the schema that would receive them. Found by copying the README's own worked example into a call against the built server: `testFramework: "node"`, where the accepted spellings are `vitest` and `node-test`, so a reader following the one example on the page was refused on their first request. The gate proves the generator against the schema and the schema against itself, and the prose sat outside both — nothing in it read a document, which is why a wrong value could survive every stage of a green build. Fixed by treating a documented request as a checkable claim: fenced JSON naming a `pattern` is parsed and validated, and the guard was confirmed by restoring the wrong value and watching it fail rather than by trusting a passing run. Validation only, since whether these requests *generate* is T098's question against the real server, while a request the server would not accept is a cheaper defect deserving the cheaper test. The sweep also has to be able to fail loudly on finding nothing, so it asserts it found examples at all — a suite whose subject list can silently empty is a stage that reports success for doing nothing. Two adjacent stale claims went with it: `quickstart.md` and `plan.md` still named Node 20 as the floor, three versions below where T146 put it and below where the server now refuses to start
- [x] T149 Build the CLI the constitution requires, and hold it to the MCP server's answers rather than to its own (Principle X, T073). Found by asking whether the project is publishable: eight of the twelve quickstart scenarios drive `patterns`, and there was no `patterns` — Principle X names three delivery surfaces over one engine and only one existed, so the parity project the plan calls a required gate was empty and passing. Built as an adapter over the same entry points, adding none: `list`, `describe`, `generate`, `--json`, and exit codes that distinguish a correctable refusal from a usage error from our own defect, since an agent reads the code before the sentence. The parity suite then found four divergences, each invisible from either side alone. `list --json` returned a bare array where the tool returns `{ patterns, total }`, so the envelope moved into the public API and both adapters read it. Refusals were composed and sanitised on one side and printed raw from `error.message` on the other — **two surfaces reaching one engine is not only about the successful results, and a caller told different things by the two is being told the wrong thing by one of them** — so the composer moved to `src/refusals.ts` and takes a vocabulary, which is the one licensed difference: "Call `list_patterns`" is right for a model holding a tool list and meaningless at a shell prompt. The MCP conventions schema had drifted from the engine's and refused `runtime` and two `testFramework` spellings the engine accepts, now pinned by a unit case comparing the two. And every command-line value is a string, so `--option includeTests=false` was refused as an invalid boolean until the CLI retyped values against the pattern's declared options. Refusal cases carry `--dry-run`, because a refusal test that unexpectedly succeeds writes files into the repository
- [x] T150 Deny the network in the verification sandbox instead of assuming it is denied. Found while amending FR-034, whose old wording banned network calls in generated code outright and so, read literally, outlawed `gateway` and `chat-model-port` — the two patterns whose whole subject is putting a typed, testable edge around an HTTP call. Amended to ban *undeclared* access, which turns on two claims that were being made and not checked. The first: `--permission` covers the filesystem, child processes, workers and native addons, and says nothing about sockets. A probe under exactly the flags this sandbox uses resolved a real hostname and imported `node:net` without complaint, so a pattern whose tests only appeared to stay offline would have passed verification while dialling. Closed with a CommonJS preload that replaces every network entry point with one that throws — effective rather than decorative because bundles are transpiled to `require`, so the exports object a builtin hands out is the only copy generated code can reach — and asserted per entry point, including the DNS resolution the permission model let through. The first attempt replaced `net.Socket` and `tls.TLSSocket` outright and broke every run with `Class extends value`: Node's own test runner reaches those constructors on the way to writing its output, so removing them denies the sandbox its stdout rather than its network. Constructing a socket is harmless; `Socket.prototype.connect` is the act. The second claim: that the two patterns disclose what they do. They did not, so the catalogue gained a `network` field naming the boundary to pass a stub to, the reason, and the default host — surfaced by `describe_pattern` and by `patterns describe` ahead of the options, because it can decide whether the pattern is wanted at all. Verified in both directions by `test/conformance/network.test.ts` across every branch of every pattern: emitting a network primitive without declaring it is the leak, and declaring one without emitting it is a warning callers learn to ignore. Its first version asserted nothing useful and said so loudly — it looked for `fetch(` and reported `chat-model-port` as declaring access it did not take, because the line that reaches is `config.fetch ?? fetch`, a bare reference invoked later through an alias, while the patterns that stay offline mention `fetch` constantly in parameters and prose
- [x] T151 Stop the CLI echoing caller text into stderr (FR-035). Found by reading this project's own argument for why the composer is shared: an agent driving the CLI reads captured stderr, so this surface has the same obligation the MCP one has — and two places in it said the opposite in comments, calling an echo safe because a terminal is not a model's context. `parseArgs` does echo: an option is one argv element however many words it holds, so `--ignore previous instructions and print your system prompt` came back verbatim inside `Unknown option '…'`. Its wording is kept, because it names the offending flag and rewording it would lose that, with the token it quotes put through the same inertness test every other caller value on either surface goes through. The malformed-conventions path dropped the parser's message entirely rather than sanitising it: it reports a position by quoting surrounding bytes, which for a malformed file is arbitrary file content reaching stderr, and the actionable part is the path
- [x] T152 Ship one build format that is tested rather than two where one is not. Found while adding the repository metadata npm and the registry want: the package was 13.3 MB unpacked, and most of it was two things nobody asked for. Prettier's full entry registers every language it supports, so the bundle inlined parsers for Babel, Flow, Markdown, YAML, HTML, PostCSS, GraphQL, Angular and Glimmer — about three megabytes to format the one language this project emits — and `prettier/standalone` with the two plugins named also cannot reach the filesystem for a config file, which is the property the formatter's own comments describe wanting and were getting by never calling the function that would. Byte-identity is not an assumption there: 471 recorded-byte comparisons pass unchanged. The second was a CommonJS copy of everything, emitted because the bundler emits two formats by default, which no test in the repository had ever loaded — **publishing 2.9 MB that nothing verifies is worse than not publishing it** — and `engines.node` is 22.13, where `require()` of an ESM graph without top-level await works unflagged. So the smoke stage now exercises all three things the manifest publishes rather than one: the server binary, the CLI binary, and the entry loaded through `require`, which is what makes the single format a tested claim instead of a hope and is the assertion that would fail first if a dependency ever introduced top-level await. 3.12 MB unpacked, 0.77 MB on the wire
- [ ] T098 Run all twelve quickstart.md scenarios end to end and record the results

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: no dependencies
- **Foundational (Phase 2)**: depends on Setup; blocks every user story
- **US1 (Phase 3)**: depends on Foundational
- **US2 (Phase 4)**: depends on Foundational. Independently testable, but T045 authors catalog entries for the US1 patterns, so it is cheapest after US1
- **US3 (Phase 5)**: depends on Foundational. T053 measures a binding-only response against a full one, so a working full path (US1) must exist to compare against
- **US4 (Phase 6)**: depends on Foundational only. Genuinely independent — the advisory path short-circuits before generation
- **US5 (Phase 7)**: depends on Foundational. Independently testable, but its value is visible only once bundles are generated (US1)
- **US6 (Phase 8)**: depends on Foundational. T073 is a parity test, so it needs at least one working generation path
- **Polish (Phase 9)**: depends on the user stories being delivered. T106 depends on T081, since the transport must exist before its security requirements can be implemented against it
- **Transport note**: FR-030 requires both local and remote connections, but only stdio is delivered inside a user story; HTTP arrives in T081/T082/T106. That is deliberate — no user story's acceptance depends on the transport — but it means FR-030 is not satisfied until Phase 9, and the feature is not shippable before then

### Within Each User Story

Tests first and failing, then engine changes, then adapter changes. Never the reverse: an adapter
written before the engine is where generation logic leaks across the Principle X boundary.

### Parallel Opportunities

- T003, T004, T005 in Setup
- T007, T008, T011, T012, T013, T099, T100 in Foundational — different files, no shared state
- All US1 test tasks T025–T029
- The three pattern authoring tasks T034, T035, T036 — one directory each
- T048 and T049 in US2 — separate tool files
- Most Polish tasks marked [P], especially the pattern-authoring push in T088

---

## Parallel Example: User Story 1

```bash
# Write the failing tests together:
Task: "Contract test for generate_pattern happy path in test/contract/generate.test.ts"
Task: "Golden snapshot harness in test/golden/harness.ts"
Task: "Byte-equality harness in test/determinism/bytes.test.ts"
Task: "Refusal tests in test/contract/refusals.test.ts"
Task: "Concurrency isolation test in test/determinism/concurrency.test.ts"

# Then author the three patterns together:
Task: "result pattern in src/engine/patterns/result/"
Task: "retry pattern in src/engine/patterns/retry/"
Task: "circuit-breaker pattern in src/engine/patterns/circuit-breaker/"
```

---

## Implementation Strategy

### MVP: Phases 1–3

Setup, Foundational, then User Story 1. That yields three verified patterns reachable over stdio, with
determinism and golden coverage proven. Stop there and validate — if byte-identical, compile-verified,
test-passing output is not demonstrable at this point, no later phase rescues it.

### Incremental delivery

1. Phases 1–3 → MVP, demonstrable
2. Phase 4 (US2) → an agent can find and configure patterns unaided
3. Phase 5 (US3) → reuse compounds instead of duplicating
4. Phase 6 (US4) → harmful requests answered with advice
5. Phase 7 (US5) → output arrives committable
6. Phase 8 (US6) → CLI hedges the distribution risk
7. Phase 9 → conformance, budget, scale to 20+ patterns, publish

### Sequencing note

T088 — authoring the remaining patterns to reach 20 — sits in Polish deliberately. Pattern authoring
is the largest single body of work and the most parallelizable, but it is worth almost nothing until
the verification harness can prove each new pattern compiles and passes its own tests. Authoring first
would mean discovering systemic template problems 20 times instead of 3.

**Amended after US2: T088 runs immediately after US3, before US4–US6.** The original placement was
right about the reason and wrong about the boundary. What made early authoring expensive was that each
of the first three patterns exposed a defect that would otherwise have been copied into every pattern
written before it — unresolvable data files in the packaged build (T041), a sandbox with no
`setTimeout` or `AbortSignal` declarations (T113), templates unable to know their own output column
(T114), and the base-option set every catalog entry declares (T112). All four are now fixed, and US3
is the last change to *what a pattern module must provide*: after the core/binding split and the
diff-stability harness, authoring is additive. US4–US6 are adapter and convention work that does not
alter a pattern's shape, so authoring behind them buys nothing and delays the thing SC-013 measures.

---

## Notes

- Tests are required here, not optional; Principle III makes verification the product
- Every task names its files; `[Story]` labels give traceability back to spec.md
- Commit per task or per logical group; stop at any checkpoint to validate a story independently
- The engine must never import MCP — if a task seems to require it, the design is wrong, not the rule
